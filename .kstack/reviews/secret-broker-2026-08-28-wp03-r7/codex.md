{"decision":"revise","confidence":99,"failedChecks":["Head validation is not stable against non-throwing stateful accessors. Both validators inspect the live object and later copy it through another series of property reads. A getter can pass validation and change during copying, causing validateAuthorityHeadValue and validateAuditHeadValue to return heads that immediately fail revalidation. Reproduced consequences include canonicalAuthorityHeadBytes emitting bytes rejected by parseAuthorityHead, authorityHeadDigest hashing that invalid head, and auditSuccessor advancing from an invalid ordinal/event-origin combination. Existing tests cover throwing accessors but miss this time-of-check/time-of-use bypass.","Fixed-error normalization remains incomplete at exported boundaries. syntheticProtectedStateSnapshotBytes evaluates attacker-controlled instanceof reflection outside a catch, so a Proxy getPrototypeOf trap escapes with its original Error and message. validateSecretUpdateId likewise destructures hostile options before entering any normalization boundary. Both raw-error escapes reproduced on the exact candidate bytes."],"securityFindings":["Stateful accessor behavior can bypass the closed authority/audit schema invariants and produce malformed canonical control-plane artifacts; the audit successor can then skip from a malformed origin representation to ordinal 2.","Exported snapshot and update-ID validation paths can expose caller-controlled raw diagnostics instead of fixed typed errors."],"materialDissent":[],"recommendation":"Snapshot/canonicalize each caller-supplied head once inside the fixed-error boundary, validate that immutable snapshot, and return or encode only the validated snapshot. Place all exported argument classification and option extraction inside fixed-error boundaries, including syntheticProtectedStateSnapshotBytes and validateSecretUpdateId. Add stateful getter/Proxy regressions asserting every returned or encoded head revalidates, plus hostile reflection tests for all exported helpers, then rerun the exact-candidate matrices and manifest checks. The two historical R6 defects themselves reproduced as closed.","strongestObjection":"The authority validator can return an epoch-2 head with GENESIS and epoch-origin fields, and its canonical codec will emit those invalid bytes even though its own parser rejects them; therefore the central closed-codec claim is currently false.","unresolvedQuestions":[]}
