# Per-item ledger: expert persona library stalled-thread remediation

**Thread:** `expert-persona-library-2026-08-23`  
**Lineage covered:** round 23 onward  
**Created:** 2026-08-25  
**Status:** living document; update in place

## Purpose

This ledger rates individual technical items and claims independently of the
aggregate confidence or disposition of the round bundle in which they appeared.
It implements the owner's 2026-08-25 process correction: retain useful
item-level evidence even when a bundle is rejected, and do not treat a bundle's
verdict as the status of every sub-claim.

Commit `dc1dbb4` is the initial inspected repository baseline. A parallel thread
advanced `main` to `af83fda` before round-24 dispatch without changing any
persona-thread, persona-catalog, panel-runner, or persona-body bytes. Before
round-25 dispatch, `main` advanced further to `6c60132` (the commit recording
this ledger's round-24 update); round 25 independently recomputed all eleven
source identities recorded in round 24's brief against that HEAD and confirmed
byte-for-byte match with zero drift. Round 25 (combined confidence 74, 12
points below round 24's 86) is rejected — recorded in
`.kstack/decisions/expert-persona-library-2026-08-23-rejected-options.md`,
entry 1 — for one unsupported absolute non-authority containment clause in its
Finding-1 closure; its other six closures (items 2-7) were independently
validated by both reviewers and were not the problem. Before round-26
dispatch, `main` advanced further to `ca3137a` (four commits, none touching
any persona-thread, persona-catalog, panel-runner, or persona-body path);
round 26 independently recomputed all eleven source identities against that
HEAD and again confirmed byte-for-byte match with zero drift. Round 26
replaced only the absolute clause with the bounded statement recorded as
entry 1's concrete alternative, kept items 2-7 exactly as round 25 drafted
them, and received Codex `approve`/92 and Opus `approve`/87 (combined
confidence 87), 1 point above round 24's 86 and 6 above the owner's 81
target, with zero failed checks, zero security findings, zero material
dissent, and zero unresolved questions from either reviewer; the whole-design
gate returned `READY_FOR_USER_APPROVAL`. Round 23 was bound to
design digest
`7f5833ac42d3dac537f05c0f938687611f1ee2ad6322351cf5351cc779656969`
and received Codex `block`/34 and Opus `revise`/68. The objective asks for a
bounded domain-expert prompt library and explicit selection for KStack
dispatches. The later-shipped virtual-panel implementation materially overlaps
that outcome: `kstack-panel-personas.mjs` provides a validated, versioned,
digest-bound catalog; four substantive bodies cover security, resilience,
compliance, and journalism; project additions/replacements are supported; and
`kstack-panel.mjs` explicitly binds selected personas into panel dispatches as
reasoning lenses with no authority. It does not complete the original
objective's direct integration with the existing design, dual-review, or QC
runners, which the panel work deliberately left unchanged. This overlap and
remaining gap are tracked as their own item rather than being conflated with
Round 23's unrelated authority-journal mechanism.

## Status meanings

- `VALIDATED` — item-specific evidence accepts the claim or classification;
  nothing further is required for that exact item.
- `REJECTED` — item-specific evidence establishes that the proposed claim or
  mechanism is unsound; the replacement is named in **Next action**.
- `OPEN-UNTESTED` — a proposal or disposition exists, but it has not been
  isolated and tested under the one-change-per-round rule.
- `OPEN-CONFIRMED-BUG` — reviewers confirmed the underlying defect or design
  gap, but no isolated accepted fix exists.

## Maintenance rule

Every future round dispatch prompt for this thread must read this ledger first.
After synthesis, update in place the status and evidence for every item that
round addressed, add every newly discovered item, and name the next isolated
round action for anything still open. Preserve material evidence when status
changes; do not recreate the file, infer sub-item status from a bundle's gate
result, or let this ledger go stale.

## Item ledger

| Item | Status | Evidence | Next action |
|---|---|---|---|
| Objective scope versus the shipped virtual-panel persona catalog | `VALIDATED` | Round 24 isolated this item at design digest `c43f16667ae380213633dcb00fefc146eca4b92b59df802994cc718ebef298d1`. Codex approved/pass at 92 with no findings, dissent, or questions. Opus approved/pass at 86 and accepted the covered-versus-remaining classification; its two low findings narrow lifecycle metadata to provenance hygiene and require future independence tests, without disputing the disposition. Combined confidence 86 exceeds round 23's 34 by 52 and the owner's 81 target by 5. All six deterministic checks passed. Round 25 (rejected, see below) attempted to close the two residual findings and five questions but regressed confidence to 74 on one unsupported absolute clause. Round 26 (design digest `707f8f5ef75573a8c883117b38cb994088fc74706fd6a59f7ea0701d2a0c81d4`) reapplied the closure with only that one clause replaced by a bounded statement, keeping items 2-7 exactly as round 25 drafted them: Codex approved/pass at 92 (zero findings/dissent/questions) and Opus approved/pass at 87 (zero findings/dissent/questions, two non-blocking suggestions), combined confidence 87 — 1 point above round 24's 86, 6 above the owner's 81 target. All six deterministic checks passed at round 26's digest. The whole-design gate now returns `READY_FOR_USER_APPROVAL` (`.kstack/reviews/expert-persona-library-2026-08-23-round26/gate.json`) — both round-24 security findings and all five unresolved questions are closed. Selection credit is explicitly limited to an opt-in, currently-disabled adjacent panel workflow. Standard design/dual-review/QC selection, comparison evidence, and seven named domain families remain open as the objective's next isolated item. | Next isolated round: standard design/dual-review catalog reuse and explicit selection contract. Answer first whether catalog loading can be reused from the existing panel module boundary (`kstack-panel-personas.mjs`/`kstack-panel.mjs`) without importing panel runtime/convergence state, or requires prior extraction into a neutral shared module. That round's own deterministic verification plan must include, as a named required release-blocking check (per round 25/26's Finding-2 closure — Opus's round-26 non-blocking suggestion was to also record this check durably here, done in this update): an automated test asserting (a) SHA-256 byte equality of the common packet across reviewers regardless of per-reviewer persona assignment, (b) manifest-disclosed per-reviewer persona ID and digest, and (c) no reviewer's prompt/transcript contains the other reviewer's raw output, recommendation, or confidence score. Do not batch persona content or evaluation into that same round. |
| Round-23 authority-journal, destructive-repair, Tier A/Tier B, and emergency-activation mechanism as the implementation path for the persona-library objective | `REJECTED` | Round 18 Opus expressly found that the thread had transformed a persona library into a mutation-authority protocol with its own journal, sparse-Merkle accumulator, offline root, three-UID process model, and launcher-mediated quiescence, without a proportionality or delivery budget. Round 23 then received Codex `block`/34 with seven failed checks and eight security findings, plus Opus `revise`/68 with nine security findings. Round-24 Codex 92 and Opus 86 both pass retirement from this objective without loss of a persona capability. The retained defect set is discoverable at `.kstack/reviews/expert-persona-library-2026-08-23-round23/codex.json`, `opus.json`, and `.kstack/reviews/expert-persona-library-2026-08-23-draft/codex-draft-r23.md`; the binding arbitration is `.kstack/decisions/fable-arbitration-persona-namespace-abandonment-round23-2026-08-23.md`. This rejection is scoped to its use for this objective. | Do not build forward from the round-23 draft or in-flight r24 three-piece namespace package in this persona thread. A future storage-security objective must explicitly import the named reports/ruling and create its own objective, locked clarification, threat model, and fresh review lineage. |
| External two-replica repair permit survives a cut of the target journal | `OPEN-UNTESTED` | Opus called the independent vault and five-point cut-survival argument sound, but both reviewers found the recovery-bearing receipt/disposition family unschematized and restart behavior incomplete. The claim was not isolated and belongs to the rejected objective-divergent mechanism. | No further persona-thread action. Reassess only under a separately chartered storage-security objective. |
| Bounded destructive disposition is restart-safe | `OPEN-CONFIRMED-BUG` | Codex's strongest objection and high findings `R23-QUIESCENCE-RESTART-DEADLINE` and `R23-FAILSTOP-DURABILITY` establish that no mandatory durable pre-attempt start/deadline or startup latch prevents deadline reset or unresolved `ISSUED` recovery. Opus also found the repair-vault receipt/disposition objects undefined. This gap specifically drove Codex's outright `block`. | Do not repair in this persona thread. If separately re-chartered, start with a durable pre-attempt object, restart-resolvable deadline, exact startup rule, and canonical signed state objects. |
| Non-destructive namespace abandonment makes the old namespace detectably non-current | `OPEN-CONFIRMED-BUG` | Opus high finding `R23-01` shows the old namespace retains a valid signed prefix with no reader-consumable retirement rule. The Fable ruling authoritatively rejects an old-namespace tombstone and requires generation, rollback-protected reader floor, and bounded-freshness attestation as an inseparable package. That ruling is unrelitigatable but remains within the rejected storage mechanism, not the persona objective. | Do not run the Fable-mandated package in this thread. Preserve the ruling for any separately chartered namespace-authority design. |
| Repair-vault durability receipts, state receipts, disposition, and fail-stop objects have normative schemas and trustworthy signers | `OPEN-CONFIRMED-BUG` | Opus high finding `R23-02` and failed-check item 1 identify missing field sets, canonical encodings, signers, keys, preimages, verification rules, and post-kill trust establishment. Codex independently found restart/fail-stop durability incomplete. | No persona-thread action. A separate design must define the complete object family and authority boundaries before reuse. |
| Three-distinct-natural-person and workflow-participant completeness claims are mechanically enforceable | `OPEN-CONFIRMED-BUG` | Codex high findings `R23-PERSON-IDENTITY-UNIQUENESS` and `R23-PARTICIPANT-COMPLETENESS`, and Opus high finding `R23-03`, agree that the directory prevents duplicate organizational subjects rather than one human enrolling through multiple subjects, while manifest completeness is recorder-attested against an unnamed audit source. | For this objective, personas remain reasoning lenses and confer no authority, so do not import this ceremony. Any future high-stakes authorization objective must either add a real uniqueness/completeness boundary or narrow its claim to proofed organizational identities with the residual disclosed. |
| External revocation is unconsumable within the claimed 30-second bound | `REJECTED` | Codex high finding `R23-REVOCATION-COMPOSED-LAG` shows allowed upstream visibility plus a fresh 30-second lease can approach 60 seconds. Opus `R23-07` adds that gateway freshness is contractual rather than proved. Both dissent from the 30-second claim. | Drop the claim in this thread. A separately chartered design must compose propagation and lease lifetime and accurately distinguish contractual source freshness from cryptographic proof. |
| Fresh-domain route and credential isolation is machine-verifiable | `OPEN-CONFIRMED-BUG` | Codex high finding `R23-STORAGE-ISOLATION-PROOF` identifies no canonical evidence schema, route universe, verifier, or durability semantics. Opus additionally shows abandonment can leave stale authority consumable. | No persona-thread action. A separate storage design must bound the route universe or narrow the supported platform rather than assert universal proof. |
| Emergency activation deadlines use bounded, rollback-safe trusted time without availability freeze | `OPEN-CONFIRMED-BUG` | Codex high finding `R23-ACTIVATION-TIME-SOURCE` and Opus high finding `R23-04` agree that `timeSourceEvidenceSha256` has no normative source/uncertainty/rollback arithmetic and that the close-receipt signer and two vault replicas become emergency-path liveness dependencies. Opus separately notes ordinary lease expiry is based on unauthenticated wall clock. | No persona-thread action. If re-chartered, specify trusted sources, uncertainty/skew, monotonic anchors, signer eligibility, and a fail-closed bounded unavailable path. |
| Single-governor emergency removal cannot deny all eligible ceremony roles | `OPEN-CONFIRMED-BUG` | Opus medium finding `R23-05` shows one governor can revoke enough minimally provisioned roles to block emergency activation while restoration needs a stronger ceremony; no quorum floor, rate limit, or restoration bound exists. | No persona-thread action. A separate governance design must define an emergency-removal quorum/floor and bounded restoration policy. |
| The 300-second quiescence classifier distinguishes proven impossibility from resource-starved noncompletion | `OPEN-CONFIRMED-BUG` | Opus medium finding `R23-06` shows an attacker can slow enumeration and deterministically force the most disruptive abandonment path because timeout is unconditional and cannot be extended or classified. | No persona-thread action. A separate design must bound work/resources and define proof-failed versus proof-unfinished disposition without reviving an unbounded negative proof. |
| At most one unexpired repair permit governs a target and crash reissue is deterministic | `OPEN-CONFIRMED-BUG` | Opus medium finding `R23-08` identifies content-addressed permits with generations but no uniqueness invariant or selection rule. | No persona-thread action. A separate repair design must define target-scoped exclusivity, supersession, and deterministic recovery selection. |
| Vault replica loss after the cut has a bounded, implementable recovery disposition | `OPEN-CONFIRMED-BUG` | Opus low finding `R23-09` notes this is fail-closed but uniquely lacks a bounded disposition; endpoint replacement is forbidden during target repair and requires an offline re-genesis ceremony. | No persona-thread action. A separate design must state an honest terminal state and operator recovery boundary. |
| Round-23 no-regression packet independently proves retained prior mechanisms and residuals unchanged | `REJECTED` | Codex medium finding `R23-NO-REGRESSION-UNVERIFIABLE` and Opus item 12 agree that prose summaries without prior canonical bytes, digests, or clause citations permit assertion review only. | Do not inherit round-23 mechanisms. Any future lineage that claims equivalence must inline or digest-bind the exact prior baseline and clause mapping. |
| Hermetic launcher closes the specified executable/configuration TOCTOU | `OPEN-UNTESTED` | Opus's recommendation says descriptor-relative open, hashing, sealed memfd configuration, `clearenv` allowlist, and `execveat` on the same descriptor close the identified TOCTOU. Codex did not dispute that sub-claim, but it was embedded in the blocked bundle and is unrelated to persona dispatch. | No persona-thread action. Preserve as non-validating evidence for a future launcher-specific objective. |
| Activation formula avoids signing nonexistent absolute deadlines and activation-core equality is internally consistent | `OPEN-UNTESTED` | Opus's recommendation says the values exist at signing time and the mutable/circular encoding defect is closed; it also accepted the activation-core equality correction. Codex's remaining time-source finding concerns trusted-time evidence, not the formula's circularity. The sub-claim was not isolated. | No persona-thread action. Preserve only as evidence for a separately scoped emergency-activation design. |
| Capability-mutex contention, freeze-fence scope, and `hardwareBootId32` encoding contradictions are corrected | `OPEN-UNTESTED` | Opus's recommendation says these contradictions were fixed without residual ambiguity, and Codex did not reopen them. They remain bundled inside an objective-divergent, blocked design. | No persona-thread action. Revalidate only if a separately scoped design actually needs them. |
| Tier A containment SLO values are selected and operational readiness is closed | `OPEN-CONFIRMED-BUG` | Codex failed check 7 and unresolved question 9 note that both required nonzero owner values remain unset; the draft itself labels readiness open. | Not applicable to a reasoning-lens persona catalog. Do not ask the owner to choose unrelated storage containment SLOs in this thread. |
| Persona expiry, revocation, and replacement metadata is tamper-evident against a project actor | `VALIDATED` | Round-24 Opus low finding `PERSONA-LIFECYCLE-NOT-TAMPER-EVIDENT` observes that these are plain fields in project-controlled JSON and can be reverted or edited with the record. The same report bounds impact because personas are untrusted reasoning lenses that confer no authority. Codex did not claim adversarial tamper evidence. Round 25 attempted to close this by rewording lifecycle metadata as a hygiene/provenance-display control and adding an absolute non-authority containment claim ("can only degrade reasoning quality, never escalate privilege, bypass a gate, or grant authority"). Opus (approve/85) accepted the closure in full. Codex (revise/74) found the absolute containment clause itself unsupported: denying personas *formal* authority does not prove persona instructions can never induce misuse of authority already available to a caller through independently enforced host/tool/gate controls the brief does not itself specify or verify — recorded as new low finding `PERSONA-NONAUTHORITY-BOUND-OVERSTATED`, one material dissent, and one deterministic `threat-model-complete` failure independently confirmed against the primary text. Combined confidence 74 is 12 points below round 24's 86; this closure attempt was rejected per the thread's confidence-regression rule and recorded in `.kstack/decisions/expert-persona-library-2026-08-23-rejected-options.md` item 1. Round 26 kept the hygiene/provenance reword and replaced only the absolute clause with the bounded statement recorded as entry 1's concrete alternative: lifecycle metadata is a cooperative hygiene/provenance-display control, not a security mitigation; a resurrected stale/reverted persona confers no *formal* tool/gate/commit/deploy/legal/authority capability, and containment against that content being acted upon through a real tool/gate/authority boundary is explicitly routed to host policy, KStack authority, skill procedure, and gate rules enforced independently of persona content and not established or verified by this classification round. Both reviewers approved (Codex 92, Opus 87) with zero findings, zero dissent, zero questions on this exact item. Opus's review states the corrected text "directly answers Codex's round-25 objection ... without conceding anything Opus accepted" and is "now consistent with the brief's own separately named persona prompt injection/authority escalation threat instead of contradicting it." Opus separately raised one non-blocking wording observation (the anti-revival rationale's "worth tampering for" phrasing should be framed around review-outcome influence, not only capability grant, if a future round restates it) — explicitly not a defect in this round's text and not material dissent. | No further persona-thread action required for this exact clause. If a future round restates the anti-revival rationale, phrase the incentive test as "nothing worth tampering for through this round's own mechanisms, given selection is digest-bound and fail-closed" per Opus's round-26 non-blocking suggestion. |
| Future standard-flow persona composition preserves blind review and byte-identical common packets with manifest-disclosed assignment | `OPEN-UNTESTED` | Round-24 Opus low finding `INDEPENDENCE-CONSTRAINT-ASSERTED-NOT-ENFORCED` accepts the constraint but notes this classification round has no mechanism or named test. No runtime path changes in round 24, so this is a future-adapter obligation rather than a current bypass. Round 25 closed the *naming* of this obligation: it states, as a named required deterministic check for the next adapter round (not narrative intent), three testable sub-assertions — (a) SHA-256 byte equality of the common packet across reviewers, (b) manifest-disclosed per-reviewer persona ID and digest, and (c) no reviewer's prompt/transcript contains the other's output/recommendation/confidence. Opus (approve/85) accepted this closure as sufficient for a classification-only round and flagged, non-blocking, that the check should also be registered in this ledger rather than surviving only in the round-25 brief's prose. Codex affirmatively accepted this closure ("The other six closures are sufficiently explicit and scoped"); its revise/74 verdict and one material dissent targeted only the separate lifecycle/non-authority row above and did not implicate this item. Round 26 carried this closure forward unchanged (per the round-26 dispatch's own instruction to keep items 2-7 exactly as round 25 drafted them); both Codex (approve/92) and Opus (approve/87) accepted it again with zero findings/dissent/questions. Opus's round-26 non-blocking suggestion was specifically to register the required check durably in this ledger row (not only in brief prose) — done in this update's Next-action column. The underlying future-adapter claim itself remains untested because no adapter exists yet. | Make this a named, release-blocking acceptance requirement in the next isolated adapter round's own deterministic verification plan: an automated test asserting (a) SHA-256 byte equality of the common decision packet delivered to each independent reviewer regardless of per-reviewer persona assignment, (b) the manifest discloses each reviewer's assigned persona ID and digest, and (c) no reviewer's prompt or transcript contains the other reviewer's raw output, recommendation, or confidence score. This test is a release blocker for that round exactly as the six checks in `workflow.designGate.requiredChecks` are blockers today, per round 25/26's Finding-2 closure. |
| Digest non-drift at current HEAD for the eleven round-24-recorded source identities | `VALIDATED` | Round-24 Opus's unresolved question asked whether the deterministic drift check actually ran. Round 25 independently recomputed SHA-256 for all eleven recorded sources (objective, Fable ruling, round-23 draft, round-23 Codex/Opus envelopes, current catalog module, current panel runner, and four persona bodies) directly against current HEAD `6c60132c6169b84ec18999e528cb9d0c55304b19` and found byte-for-byte match with zero drift. Round 26 recomputed the same eleven sources again against current HEAD `ca3137a5e7272981924f34af68b8523e02b88071` (four intervening commits, none touching any persona-thread, persona-catalog, panel-runner, or persona-body path) and again found byte-for-byte match with zero drift; the full comparison table is inlined in `.kstack/reviews/expert-persona-library-2026-08-23-round26/decision-brief.md`. Neither reviewer disputed this closure at either round. | No further persona-thread action needed for this exact digest set. Any future round must recompute again against its own dispatch-time HEAD before carrying forward confidence. |
| Ledger pointer mechanism for the retained rounds 6-23 defect set | `VALIDATED` | Round-24 Opus asked for the *mechanism* by which a future storage objective discovers the retained evidence, not just a statement that it is preserved. Round 25 named it explicitly: the "Round-23 authority-journal..." row above already carries the exact review-round paths and the Fable-ruling path in its Evidence column, and this ledger's Maintenance rule already forbids deleting or letting rows go stale. Round 25 additionally states that row's paths must never be deleted, shortened to "see history," or merged into another row even if that row's Status value later changes. Opus (approve/85) accepted this as a genuine named mechanism, not a restatement. Codex affirmatively accepted this closure ("The other six closures are sufficiently explicit and scoped"). Round 26 carried this closure forward unchanged; both Codex (approve/92) and Opus (approve/87) accepted it again with zero findings/dissent/questions. | No further action unless a future storage objective reports the pointer missing or inaccurate; if so, restore the exact paths from round 25's or round 26's decision brief or git history of this file. |
| Fable-shelving scope-decision framing (declining to implement vs. relitigating the arbitration) | `VALIDATED` | Round-24 Opus asked that the boundary between "declining to implement" and "setting aside a binding ruling" be stated explicitly. Round 25 states directly that the Fable ruling in `.kstack/decisions/fable-arbitration-persona-namespace-abandonment-round23-2026-08-23.md` arbitrated the *design* of the namespace-abandonment mechanism, not whether that mechanism belongs to the persona-library objective; scope authority over objective boundaries sits with the owner, who authorized this cleanup round; the ruling remains fully binding on that mechanism's design if a future storage/security objective separately charters it. Opus (approve/85) accepted this framing as internally coherent and sufficient to foreclose an arbiter-bypass misreading. Codex affirmatively accepted this closure ("The other six closures are sufficiently explicit and scoped"). Round 26 carried this closure forward unchanged; both Codex (approve/92) and Opus (approve/87) accepted it again with zero findings/dissent/questions. | No further action. If the owner later disputes this framing, escalate directly to the owner; this ledger row does not purport to bind the owner's own scope decision, only to record it accurately. |
| 81-target framing (reporting expectation, not a reviewer scoring anchor) | `VALIDATED` | Round-24 Opus flagged tension between a numeric confidence target inside a decision packet and independent scoring. Round 25 states explicitly that "81 or greater" is a facilitator reporting/process expectation, not a value reviewers should anchor to, and that reviewers must continue scoring on the same merits criteria regardless of the figure's presence in the packet. Opus (approve/85) accepted this framing. Codex affirmatively accepted this closure ("The other six closures are sufficiently explicit and scoped"). Round 26 carried this closure forward unchanged; both Codex (approve/92) and Opus (approve/87) accepted it again with zero findings/dissent/questions. | No further action. Any future packet in this thread that references a numeric target must route it to the facilitator's own comparison step, never to reviewer scoring instructions. |
| Next-item scoping question: catalog-loading extraction vs. reuse of the existing panel module boundary | `OPEN-UNTESTED` | Round-24 Opus asked that this question be decided or at least not silently dropped. Round 25 explicitly defers it rather than deciding it (deciding now would preempt the next round's own design work and exceed this round's scope), and names it as the single fact that most shapes the next round's scope. Opus (approve/85) accepted this as the correct call for a classification-only round. Codex did not address this closure. Round 24's Option D analysis already forecloses creating a second, competing catalog regardless of how this question resolves. Round 26 carried this closure forward unchanged; both Codex (approve/92) and Opus (approve/87) accepted it again with zero findings/dissent/questions. | The next round dispatch (the standard design/dual-review catalog reuse and selection contract item) must answer this question first, before drafting the adapter itself: can `plugins/kstack/scripts/kstack-panel-personas.mjs` catalog loading be reused without importing `plugins/kstack/scripts/kstack-panel.mjs` runtime/convergence state, or does it require prior extraction into a neutral shared module? |
