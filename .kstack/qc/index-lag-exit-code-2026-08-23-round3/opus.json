{
  "schemaVersion": 1,
  "reviewer": "opus",
  "invocationId": "cd6392e6-1f02-4694-b5d5-c1ca9f1dd0ea",
  "designDigest": "52479013c90e2242eb1aad27482d8af9e2c0e01f7a2134168c576e2c7d27f3db",
  "rawOutputSha256": "2fed19b1ac6c9e1b8e3cc3e84e5f7bce4de011b935dd3f240d98c3e0668a86fe",
  "review": {
    "decision": "approve",
    "confidence": 80,
    "failedChecks": [],
    "securityFindings": [],
    "materialDissent": [
      "Scope boundary I would not have drawn the same way: submit's retry-verification-interval wait still exits 2 (`if (!retryVerification.clear && !retryVerification.confirmed) fail(..., EXIT.STATE_ERROR)`). That is a genuinely transient 'wait and retry' condition — the exact semantic class exit 20 was introduced to name — yet an automated caller polling on 20 will see 2 there. Both doc blocks explicitly carve it out, so it is a deliberate decision rather than an oversight, but it leaves the stated invariant ('transient waits are 20, needs-attention is 2') only partially realized, which is the same critique pattern rounds 1 and 2 raised.",
      "Coverage asymmetry: the new future-dated-marker case is exercised only through `unfreeze` -> `preMutationDuplicateGate()`. The new direct `runVerification()` test uses the UNPARSEABLE marker (`at: 'not-a-timestamp'`), not a negative age. Note also that in `runVerification()` the round-3 change is effectively cosmetic — pre-round-3, `null >= minimumMarkerAgeMs` already evaluated false, so `canClear` was already false; only the audit `reason` label changes. The behavioral bug lived solely in the gate, where `null < minimumMarkerAgeMs` coerced to `0 < 30000` and routed to exit 20. The fix is placed correctly, but the direct test therefore validates labeling, not the defect."
    ],
    "recommendation": "pass (approve as-is; the follow-ups below are cheap and non-blocking).\\n\\n(1) The `age < 0` guard fully closes the named gap, and I checked the boundary values rather than taking the claim on trust. `age === 0` (marker stamped at the injected clock's exact instant) routes to `0 < 30000` -> index-lag -> exit 20, which is the correct classification: a just-written marker is genuinely young. `age === minimumMarkerAgeMs` exactly is NOT `< minimum`, so it clears — matching the documented 'at least as old as' contract, and the existing test already pins it (`clockTime = markerTime + 30000` clears). Non-finite `now`, non-finite `Date.parse(marker.at)`, non-finite `age`, and now negative `age` all return `null`, and every ambiguous case fails closed (blocked + exit 2), never fail-open to clear. Multi-marker behavior is also conservative: `youngestAt` is `Math.max` over all markers, so one future-dated marker among otherwise-old markers poisons the whole set to `null` — over-conservative, but in the safe direction.\\n\\n(2) The direct `runVerification()` test is genuinely independent — it acquires a lock via `acquireDraftLock(..., { disableHeartbeat: true })` and calls the exported `runVerification()` directly with `{ explicit: true }`, asserting the returned `clear === false` / `inconclusive === true` and the persisted audit `reason`, none of which the `unfreeze`-routed tests can reach (the gate never calls `runVerification`). It is not duplicate coverage. Its limitation is the input choice, per materialDissent.\\n\\n(3) The '9 tests' explanation is correct and sufficient. `node --test <files>` runs each file in its own subprocess and counts one top-level test per FILE; subtests roll up inside the file result and are not flattened into the summary. The isolated run (`tests: 1` for a file containing 57 `test()` blocks) is a clean demonstration of exactly that, and the 3.56s duration for that single 'test' corroborates that the file's body actually executed. I would still take one decisive artifact (below) because both prior rounds flagged it and the whole round's evidence rests on that summary.\\n\\n(4) No residual gap in the `youngestMarkerAgeMs()` contract across the callers shown. Its return domain is now `Infinity | positive-number | null`. In `preMutationDuplicateGate()` the empty/`Infinity` case is unreachable — the function returns early at `if (!unresolvedSearchEvidence(draft).length) return { blocked: false; }` before the helper is reached, so `markers` is non-empty there. In `runVerification()` the `Infinity` case (no markers at all) preserves prior clear-eligible behavior and is unchanged. One pre-existing asymmetry worth recording but out of scope for this fix: the gate considers only `unresolvedSearchEvidence(draft)`, while `runVerification()` considers `[...unresolvedSearchEvidence, ...unresolvedRetryMarkers]` — a future-dated RETRY marker is therefore guarded only on the verification path. That predates this change and is not reachable through the gate's early return.\\n\\nNo scope creep. Every changed line traces to the exit-20 classification: the helper guard, the `reason` plumbing on the gate's return object, the three call-site ternaries, the two doc blocks, and tests. The added 'poll exhaustion and search failure stay at exit 2' test is regression coverage for the new branch, not creep. The exit-inventory assertion was updated to include 20 with the no-collision check intact, and 20 avoids shell-reserved codes (126/127/128+).\\n\\nSuggested cheap follow-ups, none gating: (a) add the future-dated marker as a third case to the direct `runVerification()` test so the negative-age branch has coverage on both callers; (b) settle the test-count question decisively once with `node --test --test-reporter=tap tests/jira-queue.test.mjs` (or a one-off deliberately-failing assertion confirming `fail 1` surfaces), so future rounds stop re-litigating it; (c) the ternary `gate.keys?.length ? EXIT.DUPLICATE : gate.reason === 'index-lag' ? EXIT.INDEX_LAG_BLOCKED : EXIT.STATE_ERROR` is now triplicated across `unfreeze`/`submit`/`discard` — a small `gateExitCode(gate)` helper would prevent the three copies drifting when a fourth reason is added.",
    "strongestObjection": "The round-2 critique was that the fix repaired the named input rather than the stated invariant. Round 3 repairs the second named input (future-dated timestamps) and the invariant now holds inside `youngestMarkerAgeMs()` for all boundary values — but the invariant at the EXIT-CODE level still does not hold system-wide: `submitDraft()`'s retry-verification path returns exit 2 for a condition the doc itself describes as 'the retry verification interval has [not] elapsed', i.e. a transient wait. A caller that automates 'retry on 20, escalate on 2' will escalate a wait. The docs name this carve-out explicitly, so it is a scoping decision rather than a defect, but it is the same shape of gap the prior two rounds kept finding, and it is the reason I would not call the invariant fully realized even though I approve the diff.",
    "unresolvedQuestions": [
      "Do any other artifacts enumerate the queue exit codes? The packet asserts the cumulative diff spans exactly three files (JIRA_QUEUE.md, kstack-jira.mjs, jira-queue.test.mjs), but the frozen spec `.kstack/decisions/jira-ticket-queue-final-spec-2026-08-17.md` and any SKILL/README exit-code table are not shown. If either enumerates codes or the dry-run set, they now drift from the implementation by omitting 20. Needs a grep for the code list, not an assumption.",
      "Are queue directories ever shared across processes on machines with skewed clocks? If so, a marker written by a host a few hundred ms ahead yields a small negative age and is reported as `marker-age-unmeasurable` / exit 2 ('needs attention') for a condition that self-heals as soon as the local clock passes the marker instant. No tolerance band was considered. Harm is low (self-healing, and fails closed), but the classification is momentarily wrong in the opposite direction from the bug just fixed.",
      "Is `gate.unavailable` consumed anywhere outside the three call sites shown? The gate still returns `unavailable: true` alongside the new `reason: 'index-lag'`; if a status/reconcile path keys off `unavailable` to render a message, the index-lag case is now describable two different ways. The diff does not show that consumer.",
      "Are the audit `reason` strings ('index-lag', 'search-unavailable', 'marker-age-unmeasurable', 'marker-younger-than-index-lag-minimum') treated as a stable contract by any downstream reader, or are they internal-only? The three call-site ternaries now depend on the exact literal 'index-lag' with no shared constant, so a typo in a future edit silently degrades exit 20 back to exit 2 with no test failing unless that specific path is covered."
    ]
  }
}
