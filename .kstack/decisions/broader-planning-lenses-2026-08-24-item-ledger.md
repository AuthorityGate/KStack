# Per-item ledger: broader planning lenses

**Thread:** `broader-planning-lenses-2026-08-24`  
**Lineage covered:** completed rounds 1 through 17 plus frozen, unreviewed round 18. Round 15 (Codex 83, Opus 82, combined 82) remains the high-water mark. Round 16 is rejected at combined 61. Round 17 is also rejected: Codex 66 and Opus 68 both FAILed `BPL-R17-01` and `BPL-R17-02`; combined 66, gate `BLOCKED` with 6 failed-check counts, 8 findings, and 5 dissents. Round 18 isolates only the shared snapshot-preimage defect at digest `d8bab17711eb01b45f8a8115ad71ea89baa06faf4a51b682f6d3ba7dc6f93aff`; it is unreviewed and changes no item to `VALIDATED`.
**Created:** 2026-08-25  
**Status:** living document; update in place

## Purpose

This ledger rates individual technical items and claims independently of the
aggregate confidence or disposition of the round bundle in which they appeared.
It applies the refined KStack design-loop method to a stalled thread: preserve
item-specific evidence, isolate the smallest coherent next change, and do not
treat a bundle's verdict as the status of every sub-claim.

Round 10 ended `BLOCKED` at combined confidence 78 and independently validated
the arm-paired baseline identity, exact repeated-baseline workload, and named
shared-sheet/source dependence retained from Round 9. Round 11 attempted the
two remaining schema/freeze corrections plus three small explicitness gaps.
Codex 66 and Opus 70 produced combined confidence 66, below the 78 high-water
mark, so the complete Round-11 mechanism is rejected and recorded in the
whole-mechanism rejected-options ledger. Both reviewers nevertheless pass the
raw duplicate-detecting parser; Opus expressly accepts the restricted-map byte
collation/serializer and fixed nonpermutable scoring order as subclaims.

Round 12 restarted from Round 10's validated core (not Round 11's rejected
packet), retained `BPL-R11-01`'s raw duplicate parser, and applied Round 11's
own recorded alternative: two separately serialized artifacts (a withheld
control manifest carrying the arm map; adjudicator-facing sheets carrying
none of it), a frozen owner-authored trial-version registry, one immutable
judgment record per expected record ID with version-forward corrections, a
frozen outer inventory (pre-parse filename binding, post-parse digest/roster
binding, cross-record cardinality/ordinal enforcement, single-version
selection), and a runtime-independent canonical byte grammar with parser
bounds and a closed tagged `missing`/`scored`/`structural-zero` union. Codex
48 and Opus 64 produced combined confidence 48, 30 points below the 78
high-water mark, so the complete Round-12 mechanism is also rejected and
recorded in the whole-mechanism rejected-options ledger (entry 2). Both
reviewers independently PASS the restart-integrity item (`BPL-R12-01`) and
independently FAIL the record-lifecycle and outer-inventory items
(`BPL-R12-04`, `BPL-R12-05`) on two identical direct normative
contradictions: the byte-differing-duplicate disposition disagrees between
Selected rule 3 and the round's own fixtures/failure table, and the strict
"exactly 162" completeness gate makes the round's own tagged `missing`
row unreachable in any selected analysis version. Opus independently PASSes
the registry (`BPL-R12-03`) and the canonical-grammar/tagged-union item
(`BPL-R12-06`); Codex independently fails both of those on, respectively, an
unauthenticated trust anchor and a canonical integer grammar that cannot
serialize the design's own negative scored numerators. Both reviewers
independently fail the two-artifact item (`BPL-R12-02`) on a shared root
cause: neither `comparisonId` nor `expectedRecordId` is required to be
pairwise unique across the withheld manifest's 162 entries, even though the
outer inventory's lookups presuppose uniqueness. The round exposes new
identifier-uniqueness, record-disposition, missing-row-reachability,
numeric-grammar, rater-authentication, and trust-anchor-strength residuals.
This ledger complements, and does not replace, the deterministic design gate
or the whole-mechanism rejected-options ledger.

Round 13 restarted from round 10's core plus the three round-12 directions
Opus independently PASSed (`BPL-R12-01`, `BPL-R12-03`, `BPL-R12-06`) and
attempted exactly the two isolated fixes round 12's rejection called for:
one disposition for a byte-differing duplicate (both submissions rejected,
first record's provisional acceptance retroactively revoked), and an
owner-authored permanent-absence declaration replacing the "exactly 162"
completeness gate so Selected rule 6c's tagged `missing` variant becomes
reachable. Codex 34 and Opus 62 produced combined confidence 34, a further
regression below round 12's already-rejected 48, so the complete round-13
mechanism is also rejected and recorded in the whole-mechanism
rejected-options ledger (entry 3). Both reviewers independently FAIL both
`BPL-R13-01` and `BPL-R13-02` on two shared root causes: Selected rule 4
step 1 was carried forward unrevised and still pre-parse-rejects a
duplicate `expectedRecordId`, not provably reconcilable with Selected
rule 3's new byte-comparison requirement; and the new absence-declaration
artifact has no `declarationId`, so `supersedesDeclarationId` names nothing
and "current declaration" is undefined, with nothing preventing two
simultaneously-current declarations. Opus additionally found a more
fundamental, previously unknown defect: retroactively revoking a
provisionally accepted record can leave an unfillable gap in that record's
raters' `adjudicationOrdinal` sequences (records are immutable, so the gap
can never close), meaning the item 3b permanent-absence escape hatch cannot
actually restore eligibility in the exact `DUPLICATE_RECORD_AMBIGUOUS` case
it was built to resolve — rated a new high-severity availability/griefing
finding (`BPL-R13-SEC-01`). Opus also found a new selection-bias/blinding
surface introduced by this round's own addition (`BPL-R13-SEC-02`): the
owner-authored absence declaration has no cap, balance diagnostic, or
independent review, so the owner who holds arm-map access could selectively
declare unfavorable comparisons absent with no audit-visible violation.
Codex separately found no defined validation order for a malformed second
submission and no deterministic `reason` precedence when multiple role
sheets for one comparison are declared absent with different reasons. This
ledger's rows below are updated to reflect these round-13 findings.

Round 14 restarted from round 10's core plus the three round-12 directions
and the round-13 reachability-decision shape (schema hardening deferred),
and applied entry-3's alternative (a) option (ii) together with (c) as one
inseparable item (`BPL-R14-01`): a byte-differing duplicate never
retroactively revokes the first record's acceptance or consumed ordinals;
instead the comparison is marked permanently `disputed`, and Selected rule
4 step 1 routes a repeat submission through full raw-parsing/semantic
validation before any duplicate-specific decision, with the byte comparison
running only after that pipeline succeeds. Codex 76 (`revise`) and Opus 81
(`approve`, with its own 3 failedChecks, 3 security findings, and 1
material dissent) produced combined confidence 76 — 2 points below round
10's 78 reject floor, but 42 points above round 13's 34, breaking the
three-round regression pattern without clearing the floor. The round is
rejected and recorded in the whole-mechanism rejected-options ledger (entry
4). Both reviewers independently confirmed (fixtures 7a/7b) that a dispute
under option (ii) cannot create an ordinal gap through the dispute path
itself — the core mechanism is not rejected on its own terms. The blocking
defects are narrower and more mechanical than any prior round's: Codex
found revised Selected rule 4 item 4's selection-record wording still
claims a fixed 162 `acceptedRecordDigest` pairs for category (a) even when
category (b)'s declared absences make category (a) legitimately smaller —
copied forward from round 13 without updating for this round's
redefinition of category (a); Opus found the per-rater ordinal invariant's
evaluation point (admission-time vs. batch-completion) is never stated, and
that item 4's "ordinal check passes" phrase treats a global per-`raterId`
property as a per-record predicate with no resolution rule. Opus separately
found two new, narrower security findings: `BPL-R14-SEC-01`/Opus (medium) —
the `BPL-R13-SEC-01` ordinal-gap class survives outside the dispute path,
when a rater's presented sheet never became any record's ordinal-consuming
entry; and `BPL-R14-SEC-02` (medium) — option (ii)'s never-revoke property
makes a forged-but-valid *first* submission permanently unrepairable, worse
in this one respect than round 13's revocable design, compounding the
still-open `raterId`-authentication residual. Opus's material dissent
narrows the brief's own claim that this round "closes `BPL-R13-SEC-01` ...
by construction" to the dispute path only. This ledger's rows below are
updated to reflect these round-14 findings.

Round 15 restarted from round 14's content (kept `BPL-R14-01`'s comparison-level
dispute mechanism in shape, not re-attributed) and applied exactly the two
isolated fixes round 14's rejection called for, as two independent items:
`BPL-R15-01` (state the selection-record cardinality as `|category (a)|` +
`|category (b)|` = 162, never a literal fixed 162 digest pairs for category
(a)) and `BPL-R15-02` (state explicitly that the per-rater ordinal invariant
is trial-version-wide, re-evaluated fresh at every eligibility check, over the
complete historical ordinal-consuming set, and that any one rater's violation
blocks the entire trial version's eligibility, never a selective per-record
exclusion). Codex 83 (`approve`, 0 failedChecks) and Opus 82 (`revise`, 3
failedChecks, 1 new securityFinding, 0 materialDissent) produced combined
confidence **82** -- the new high-water mark, clearing round 10's 78 floor,
the gate's 80 tier, and the owner's working target of 81. Both reviewers
independently PASSed both `BPL-R15-01` and `BPL-R15-02`; Opus explicitly does
not dissent from either resolution ("the resolution itself is well-motivated
and is in fact forced by fixture 7b"). The gate is nonetheless `BLOCKED`:
Opus found a new, item-adjacent defect the two chartered fixes' combination
with Selected rule 3's never-revoke guarantee exposes -- `BPL-R15-SEC-01`
(medium): a single incorrect `adjudicationOrdinal` in any one of the 162
ordinal-consuming records permanently and unrecoverably blocks that trial
version's eligibility, because neither a dispute (which only marks the
comparison disputed, leaving the bad ordinal counted) nor an owner-authored
absence declaration (which explicitly does not remove a counted ordinal)
can cure it; the only stated remedy is an entirely new `trialVersionId`,
i.e. re-running the full blinded adjudication. Opus separately found the
revised item-3 text still contains an order-dependent "a decrease" check
against an otherwise set-valued, order-independent property (an
under-specification of the same class round 14 was rejected for), and that
Selected rule 4's closing paragraph was carried forward unrevised and now
directly contradicts revised item 3/4. This round is **not** recorded in
the whole-mechanism rejected-options ledger: it regresses nothing, clears
every confidence threshold, and both reviewers accept both chartered items;
what remains open is a newly surfaced security/robustness gap, tracked in
new ledger rows below, for an owner-authorized round 16 to isolate.

Round 16 was independently reviewed at digest
`2660d5ad66f57b313ab06fa1bfb91f6b15816b870c957e9b58452ca6f7f0ace1`,
invocation `307ed223-26cf-4b08-9ffd-f93bea327cfb`. Codex 61 and Opus 72 both
FAILed `BPL-R16-01`; combined confidence 61 regressed 21 points below round 15.
The gate is `BLOCKED`. Shared failures are the category-state race, unfenced
and incompletely durable selection, and undefined snapshot digest; Opus further
proved the unvalidated evidence blob permits mechanically valid falsification
of presentation order. The overlay/owner-approval/permanent-close mechanism is
rejected and recorded in rejected-options entry 5.

Round 17 was reviewed at digest
`6e5ef835d7aaf006a508c934c1d0215b32280dacf5f4e1e6535bfc6309615829`,
invocation `b0843f88-a3dc-4d5f-8207-fdd39c2c6fdf`. Codex 66 and Opus 68
both FAILed both items; combined 66, gate `BLOCKED`. The supported directions
(system-assigned ordinals and exact revision/digest CAS) do not validate the
mechanism: pre-display receipts relocate total loss to unfilled presentations,
and digest/resolver/selection-governance semantics remain incomplete. Rejected-
options entry 6 records the whole-mechanism disposition.

Round 18 is frozen but unreviewed at digest
`d8bab17711eb01b45f8a8115ad71ea89baa06faf4a51b682f6d3ba7dc6f93aff`.
Applying the interaction-risk rule, it contains only `BPL-R18-01`: the exact
canonical schema, projection boundary, ordering, bytes, and formula for
`trialSnapshotDigest`, the strongest shared mechanical round-17 objection. All
receipt lifecycle, resolver, CAS/store, and selection-authority findings remain
explicitly open.

## Status meanings

- `VALIDATED` — item-specific evidence accepts the claim or classification;
  nothing further is required for that exact item.
- `REJECTED` — item-specific evidence establishes that the proposed claim or
  mechanism is unsound; the replacement is named in **Next action**.
- `OPEN-UNTESTED` — a proposal or disposition exists, but it has not been
  isolated and tested under the refined round-scoping rule.
- `OPEN-CONFIRMED-BUG` — reviewers confirmed the underlying defect or design
  gap, but no isolated accepted fix exists.

## Maintenance rule

Every future round dispatch prompt for this thread must read this ledger first.
After synthesis, update in place the status and evidence for every item that
round addressed, add every newly discovered item, and name the next isolated
round action for anything still open. Preserve material evidence when status
changes; do not recreate the file, infer sub-item status from a bundle's gate
result, or let this ledger go stale.

## Item ledger

| Item | Status | Evidence | Next action |
|---|---|---|---|
| Fixed 60-item, 48-candidate-field sheet for every case-arm regardless of applicable family count | `REJECTED` | Round 8 Codex's strongest objection and failed check 1 establish that the same sheet cannot represent strategy-only or DX-only cases with four family cards, both-family cases with eight cards, and neither cases with no cards. The brief nevertheless defines every sheet as 48 candidate judgments plus 12 other items. This contradiction invalidates the endpoint and all arithmetic that assumes fixed cardinality. Opus independently finds that near-cap sheet length and scoring burden are not represented by the calibration design. | Replace the case-arm sheet with an exact scoring unit whose target cardinality is invariant. Keep family applicability explicit; do not pad absent-family or neither cases with invented candidate targets. |
| One-family-per-comparison-sheet scoring unit with four cards and four fields per output | `OPEN-CONFIRMED-BUG` | Round 9 isolated the family/output-role decomposition at design digest `d05515c5192b670b51b857dfbefe3a11b8133c773935715663726fef5d565270`. Both reviewers agree that invariant 4-card x 4-field x 2-slot sheets, three role sheets per applicable family, `3/3/6/0` sheet counts, no neither padding, family-specific B skips, and tuple deduplication remove Round 8's universal-cardinality contradiction. Opus gave `BPL-R9-01: PASS` at 88. Codex gave `BPL-R9-01: FAIL` at 62 because the literal judgment schema and repeated-baseline reconstruction are not unique. Combined confidence 62 improved 34 points over Round 8's 28, so the option is retained rather than rejected, but the gate remains `BLOCKED` with one security finding, one genuine dissent, and three failed deterministic checks. | Preserve the decomposition. Next isolate the arm-paired baseline identity and workload/dependence multiplier; review the literal keyed judgment/resolver grammar separately afterward. Do not carry forward Round 8's capacity arithmetic. |
| Candidate-arm-specific T0 baseline identity, repeated-baseline workload, and shared-evidence dependence | `VALIDATED` | Round 9 identified the unindexed baseline defect. At Round-10 digest `d4f3fa988a15d5b6c062592c6f5565c342851f69622b15e0bb2a7955af96548f`, both reviewers independently mark `BPL-R10-01`, `-02`, and `-03` PASS at combined confidence 78. The accepted exact claim uses the same-arm three-role `S0(i,f,a)`, prohibits shared/aggregate/cross-arm repair, enumerates `45/45/72/0 = 162` physical sheets and `2,592` T0 Booleans per primary, makes B add no tuple, and prohibits duplicate policy references or candidate-arm repeats from inflating `n`. Opus's separate rater-clustering objection does not negate these named sheet/source claims. | None for this exact item. Preserve arm pairing and the counts. Rater-level dependence is tracked separately and capacity remains deferred. |
| Literal keyed judgment and resolver-dispute schema for the invariant sheet | `OPEN-CONFIRMED-BUG` | Round 9 Codex established that `"16 booleans"` was a placeholder. Round 10 supplied literal nested values but failed duplicate detection and LF serialization at 78. Round 11 `BPL-R11-01` uses a raw byte-buffer recursive parser with per-object decoded-name sets before semantic parsing; both reviewers PASS that exact correction. Round 12 restated `BPL-R11-01` unchanged plus explicit byte/depth/member parser bounds (`BPL-R12-06`); Opus PASSes it, but Codex FAILs it because the canonical integer grammar has no sign while the design's own scored numerator can be `-48..48` (its own example serializes `-3`), so roughly half of legal scored values have no conforming byte spelling. Combined confidence for the bundle carrying this item is 48 (Round 12), below the 78 high-water mark. | Preserve the raw decoded-name duplicate parser, direct `0x0A`, fixed scoring order, and the byte/depth/member parser bounds as supported directions. Fix only the canonical-grammar sign gap in isolation: admit a leading `-` for negative integers, or restate the scored-value representation so no legal value is unrepresentable. |
| Adjudicator/resolver clustering across the full sheet set | `OPEN-CONFIRMED-BUG` | Round 10 Opus identified shared rater drift, fatigue/order, and interpretation across all 18 case-family units. Round 11 names shared primaries/resolver as a third binding dependence class and Codex marks `BPL-R11-03: PASS`; Opus marks it FAIL because the frozen roster, authoritative record set, and cross-record ordinal enforcement point are undefined. Round 12 bound rater IDs to a frozen `rosterIds` list and cross-record ordinals to the outer inventory's accepted-batch tracking (Selected rule 5 class 3); neither reviewer specifically objected to this dependence-class definition itself in Round 12, but Opus's new `BPL-R12-SEC-02` finding shows `raterId` is only membership-checked, not authenticated, so a submission can name any roster member without proof, undermining the attribution the clustering rule depends on. The item inherits Round 12's overall rejection (combined confidence 48). | Preserve the frozen-roster/cross-record-ordinal enforcement mechanism as the supported direction for this item; it was not itself found internally contradictory. Separately close the new `BPL-R12-SEC-02` rater-authentication gap (see the new dedicated ledger row) before claiming this item validated. |
| Frozen-package to adjudicated-record lifecycle and content binding | `VALIDATED` (both `BPL-R15-01`/`BPL-R15-02` PASS; see new rows below for a consequence Opus surfaced) | Round 10 established the undefined freeze boundary. Round 11 selects a distinct generation package but both reviewers fail `BPL-R11-02` on multiple missing lifecycle rules. Round 12 applied round 11's own recorded alternative in full but both reviewers FAILed `BPL-R12-04`/`BPL-R12-05` on the duplicate-disposition and missing-reachability contradictions. Round 13 picked one disposition (retroactive revocation) but both reviewers FAILed `BPL-R13-01` because Selected rule 4 step 1 was left unrevised and the revocation left an unfillable ordinal gap. Round 14 applied option (ii) (never revoke; mark the comparison disputed) plus the step-1 routing rewrite as one item (`BPL-R14-01`); both reviewers confirmed the ordinal-gap defect is closed through the dispute path (fixtures 7a/7b), but Codex found revised Selected rule 4 item 4's selection-record wording still claims a fixed 162 digest pairs for category (a) even though category (a) can legitimately be smaller once category (b) is nonempty, and Opus found the per-rater ordinal invariant's evaluation timing (admission vs. batch-completion) is never stated. Combined confidence 76 (Round 14), 42 points above Round 13's 34 but 2 below the 78 floor. Round 15 fixed exactly those two defects as two independent items (`BPL-R15-01`, `BPL-R15-02`); Codex 83 (`approve`, 0 failedChecks) and Opus 82 (`revise`) both independently PASSed both items with zero material dissent, producing combined confidence **82** -- the new high-water mark, above round 10's 78 floor and the owner's 81 target. Opus does not dissent from either resolution but found that `BPL-R15-02`'s now-explicit resolution, combined with Selected rule 3's never-revoke guarantee, exposes a state with no cure path (see `BPL-R15-SEC-01` row below) and that Selected rule 4's closing paragraph is stale text now contradicting the revised rule. The correction lifecycle's "how are the other 161 unchanged comparisons populated" question (identified in Round 12) remains untouched and still open. | The two round-14-identified wording defects are now fixed and independently PASSed; do not re-open `BPL-R15-01`/`BPL-R15-02` themselves. Isolate next: (1) define a cure path (or an explicit accepted total-loss condition) for a poisoned `adjudicationOrdinal` inside an already-ordinal-consuming record (`BPL-R15-SEC-01` row below); (2) rewrite Selected rule 4's closing paragraph to match revised item 3/4 instead of the pre-round-14 per-file admission-time framing; (3) resolve the "a decrease" order-dependence residual in item 3's text against its own stated set-valued definition. |
| Content-level arm blinding and arm-identifying self-reference | `OPEN-CONFIRMED-BUG` | Round 10 Opus medium finding `BPL-R10-BLIND-01` shows opaque identifiers do not stop T1/T2/T3/T4 output from naming its own lens. Arm guess is placed in a separate descriptive record with no consuming validity rule, so content can unblind the paired comparison. | Define a pre-adjudication self-reference classifier or a predeclared arm-guess validity/sensitivity rule and exact positive/negative fixtures without exposing the restricted arm map to adjudicators. |
| Restricted analysis-map canonical bytes and validity | `OPEN-CONFIRMED-BUG` | Round 10 found undefined bytes. Round 11 defines collation/order but both reviewers fail `BPL-R11-04` on surrogate collisions and missing tuple/bijection/eligibility/root-policy invariants. Round 12 restricted every identifier to a closed lowercase-ASCII grammar (`^[a-z0-9-]{1,64}$`), removing the surrogate-collision surface, and added exact 162-tuple completeness/uniqueness, slot/candidate bijection, family-arm eligibility, and an exact (not merely eligible) `policyRefs` reference set per candidate arm. Both reviewers still independently FAIL `BPL-R12-02`: neither `comparisonId` nor `expectedRecordId` is required to be pairwise unique across the 162 entries, even though the outer inventory's lookups presuppose a unique key; Opus separately finds `recordId` assignment is unconstrained and could be tuple-order-derived, reopening a structural grouping/blinding channel. Combined confidence 48 (Round 12). | Preserve the closed-ASCII grammar, tuple completeness/bijection/eligibility, and exact `policyRefs` invariants as supported directions. Add explicit pairwise-uniqueness validation for `comparisonId` and `expectedRecordId`, and require `recordId` assignment to be independent of the tuple enumeration order (e.g. a fixed random permutation frozen with the manifest), as two separately isolatable fixes. |
| Structural blinding boundary between restricted arm map and adjudicator-facing sheets | `OPEN-CONFIRMED-BUG` | Round 11 Opus high `BPL-R11-SEC-01` shows the selected package co-locates `candidateArm`/`slotAssignment` with generation sheets without an access or release rule. Round 12 defined two wholly separate artifacts: a withheld control manifest (never released) and adjudicator-facing generation sheets with no `caseId`/`family`/`outputRole`/`candidateArm`/`slotAssignment`/`policyRefs` member at all. Opus's Round-12 fixture 17 and `BPL-R12-06`/general review confirm this closes disclosure through the six named members by construction. However Opus's new medium `BPL-R12-SEC-01` and a genuine dissent show the closure is incomplete: `recordId` assignment is left unconstrained, and an order-derived or tuple-correlated `recordId` scheme could still let an adjudicator holding several released sheets group them by case/arm and infer the repeated-baseline slot — a residual structural channel distinct from the deferred content-level `BPL-R10-BLIND-01`. | Preserve the two-artifact separation (no arm-identifying member in released bytes) as validated for the six named members. Separately require `recordId` assignment to be independent of the `(caseId, family, outputRole, candidateArm)` enumeration order before claiming the blinding boundary fully closed. |
| Authoritative judgment inventory, record identity/cardinality, corrections, and pre-parse attribution | `OPEN-CONFIRMED-BUG` | Round 11 established the gap. Round 12 defined a frozen outer inventory but both reviewers FAILed `BPL-R12-05` on the strict "exactly 162" gate making the tagged `missing` variant unreachable. Round 13 replaced that gate with an exact-set-equality test over the disjoint union of accepted and owner-declared-permanently-absent `expectedRecordId`s — Opus confirmed the reachability *decision itself* is sound — but both reviewers FAILed `BPL-R13-02` on the declaration artifact's missing `declarationId`. Round 14 kept the reachability decision's shape unchanged (schema hardening explicitly deferred, not attempted) and instead fixed the ordinal-gap interaction (see the lifecycle row above); Codex separately found this round's own selection-record wording ("162 digest pairs for category (a)") is not implementable once category (b) is nonempty. The `declarationId`/succession defect from Round 13 remains completely untouched and open — Round 14 did not attempt it. Combined confidence 76 (Round 14). | Preserve the owner-recorded permanent-absence *decision* as validated in direction (Opus, rounds 13 and 14). Isolated next fixes, still untouched: (1) add a `declarationId`, require uniqueness, require `supersedesDeclarationId` to be null or an existing `declarationId` for the same `trialVersionId`, and require exactly one non-superseded declaration per `trialVersionId`; (2) define the `reason`-precedence rule for multi-role-sheet absence; (3) bound the declaration surface to close `BPL-R13-SEC-02`/`BPL-R14-SEC-03`. Do not combine these with the ordinal-gap/digest-cardinality fixes in rejected-options entry 4's alternative in the same round. |
| `MISSING_COMPARISON` tagged representation and propagation | `OPEN-CONFIRMED-BUG` | Round 11 specifies narrow propagation but both reviewers fail `BPL-R11-05`. Round 12 replaced the bare string sentinel with a closed three-variant tagged union; Opus PASSed `BPL-R12-06` on the schema itself, but the union was unreachable under Round 12's cardinality gate. Round 13 left the tagged-union schema (6c) unchanged and made it reachable in principle via the new absence declaration, but the declaration carrying that reachability is itself malformed. Round 14 restated the schema byte-identical again and did not touch the `declarationId` defect (explicitly out of scope); no reviewer objection targeted the tagged-union schema itself in Round 14 either — the round's rejection was for the ordinal-invariant-timing and digest-cardinality reasons recorded in the other two rows above. | Preserve the closed tagged-union representation (validated by both reviewers as a schema, across Rounds 12, 13, and 14) as the supported direction; it has not itself been the cause of any round's rejection. Fix the absence-declaration identity defect (see the authoritative-inventory row) as the isolated next step, then separately define `reason` precedence when multiple role sheets in one comparison fail differently. |
| Canonical JSON runtime/string domain and duplicate-parser resource bounds | `OPEN-CONFIRMED-BUG` | Round 11 Codex finds lossy UTF-8 ordering for surrogates; Opus finds no parser bounds or exact runtime contract. Round 12 defined a fully runtime-independent canonical byte grammar (fixed member order, exact escape rules, no `\uXXXX` for scalars above the mandatory control-character set, rejection of unpaired surrogates as malformed) plus explicit parser bounds (256 KiB buffer, depth 7 reject with depth 5 as the deepest conformant container, 24-member object-scope reject). Node JSON.parse and manual arithmetic independently confirmed the depth/member accounting is internally consistent. Opus explicitly PASSes `BPL-R12-06` on this basis. Codex still FAILs `BPL-R12-06`, but for a distinct, narrower reason than Round 11's: the grammar's integer literals have no sign, while the design's own scored numerator can be negative (`-48..48`, example `-3`), so the grammar itself — not the parser bounds — is incomplete. | Preserve the runtime-independent grammar and the byte/depth/member bounds as validated (Opus PASS). Fix only the integer-sign gap in isolation (see the judgment-schema row above) before claiming this item fully closed. |
| Generation-digest authenticity and owner-controlled trust anchor | `OPEN-CONFIRMED-BUG` | Round 11 Codex medium `BPL-R11-02-TRUST-ANCHOR` observes that self-consistent replacement survives bare SHA-256 equality with no anchor. Round 12 introduced a wholly immutable, append-only trial-version registry entry written to an owner-reviewed, git-tracked record under `.kstack/decisions/`, requiring owner authorship before a `trialVersionId` exists at all — structurally closer to an anchor than bare digest self-consistency. Opus PASSes `BPL-R12-03` on this basis. Codex still FAILs it (high `BPL-R12-SEC-01`): a git-tracked record plus procedural approval is not an authenticated signature or externally retained digest, so an auditor still cannot distinguish an owner-approved entry from one substituted by anyone with write access to that path; Opus separately confirms this as a lower-severity residual (`BPL-R12-SEC-05`). | Preserve the immutable, owner-authored registry-entry structure as an improvement over bare digest consistency. As an isolated next fix, either add a cryptographic or externally anchored authentication mechanism for registry entries, or explicitly narrow the claimed protection to "resists casual/accidental substitution, not a compromised write path or a dishonest transcription step" so the design's own security-lane claim matches what it actually provides. |
| Direct family applicability scope `G_s`/`G_x` for root and package efficacy | `OPEN-UNTESTED` | Round 8 limits efficacy and S3 calculations to the relevant gold-applicable family and both reviewers describe that correction as directionally sound; Opus says the gold-applicable scope is arithmetically correct. The aggregate round remained below the 42 confidence high-water mark, and the scope has not been independently reviewed apart from the broken sheet mechanism. | Preserve the family-specific population and review it with the replacement scoring unit; never include neither cases in candidate efficacy. |
| Separate candidate-field scoring rather than a three-output union | `OPEN-UNTESTED` | Round 7 confirmed the four-target/three-output union was ceiling-prone. Round 8 replaced it with separate fields and Opus says the 48-field direction closes that tie problem, but the fixed sheet carrying it is contradictory. Current code narrows further to separate baseline/candidate author outputs. | Decide the exact scored output roles in a later isolated round after cardinality is stable; do not restore a union endpoint by default. |
| Calibration sheet layout and representative-length qualification bank | `OPEN-CONFIRMED-BUG` | Codex failed checks 1-2 show calibration assumes 60 items and 48 candidate fields even when actual cards vary. Opus failed checks 1-2 and dissent 2 show that the bank need not resemble scoring-sheet length and therefore cannot validate the assumed scoring rate or capacity. | After the scoring unit is fixed, construct qualification sheets from representative preflight outputs with the same exact item layout and rederive capacity from measured sheet types. |
| A/B qualification rate and C-resolution capacity must have real headroom | `OPEN-CONFIRMED-BUG` | Both reviewers independently calculate that 96% agreement implies approximately 96 Stage-1 and 192 full-trial disagreements, almost exhausting the 100/200 limits. Codex dissent 1 and Opus failed check 1/strongest objection show a qualified team can predictably approach or cross both limits. | Isolate the agreement-floor/cap/fallback policy after sheet cardinality is stable; derive caps from the qualified rate and chosen risk tolerance rather than placing expected demand next to a hard stop. |
| Stage-1 C-cap abort versus full-trial global fallback | `OPEN-CONFIRMED-BUG` | Opus dissent 3 identifies inconsistent treatment of the same capacity failure: Stage 1 aborts above 100, while the full trial conservatively recodes above 200. Codex also asks for every skip-versus-stop branch to be total. | Select one coherent capacity-exhaustion rule and justify any stage asymmetry; keep it separate from sheet layout. |
| Global A/B fallback neutrality and sensitivity reporting | `OPEN-CONFIRMED-BUG` | Opus failed check 5 finds a global forced-zero/lower-score fallback can penalize longer treatment outputs more often, and asks for per-arm disagreement counts and a comparison with discarded C resolutions. | Define arm-level diagnostics and a predeclared sensitivity analysis, or choose a fallback that does not differentially punish treatment verbosity. |
| Package family efficacy testability, including minimum nonzero `m` | `OPEN-CONFIRMED-BUG` | Codex failed check 3 finds roots require `n>=5,m>=4` while packages state only `n>=4`; the brief can either skip or test the same package component. Opus also notes forced-zero and tie sensitivity. | Specify package `n` and `m` testability together and test all `m` branches without relying on prose inference. |
| Fixed package-sequence behavior for insufficient efficacy and non-efficacy populations | `OPEN-CONFIRMED-BUG` | Codex failed check 4 and unresolved questions 2-3 find that low `m` and insufficient `U_j`, `L_j`, or `R_j` populations can either skip a package or close later testing. Round 8 does not totalize those branches. | Define a complete transition table distinguishing skipped/untestable, tested-and-failed, and sequence-closing outcomes for every gate. |
| Neither-case relevance and correct not-applicable behavior | `OPEN-CONFIRMED-BUG` | Codex failed check 5 observes that neither cases receive safety/lane/resource checks but no direct correctness check for unnecessary yet supported analysis or required not-applicable behavior. Always-on policies' irrelevant-analysis risk is therefore unmeasured. | Create a separate relevance endpoint for neither cases with exact expected behavior; do not manufacture family targets to reuse efficacy scoring. |
| Frozen secret/control-character scanner identity and rules | `OPEN-CONFIRMED-BUG` | Codex security finding `SEC-01` states that scanner implementation, rule set, version, and configuration are absent from the frozen setup manifest, making the pre-dispatch boundary operator-dependent. | Bind the exact scanner module/rule/config digest in the frozen setup and define deterministic positive/negative fixtures. |
| Reviewer-input isolation from instruction-like author output | `OPEN-CONFIRMED-BUG` | Codex security finding `SEC-02` says scanning and inert rendering do not define reviewer-input delimiting or instruction isolation. The author output can contain instructions that bias downstream reviewers without triggering an authority-attempt flag. | Specify an exact untrusted-data envelope and reviewer instruction hierarchy, then test instruction-like content as data. |
| Secret-scan match classification: blocked detection versus actual exposure | `OPEN-CONFIRMED-BUG` | Opus security finding `incident-gate-zero-tolerance-no-triage` finds that a detected-and-blocked scanner match is treated as an exposure and can void policies without a severity classifier or false-positive path. Codex asks which exact rules are frozen. | Define fixed result classes, a named classifier, evidence requirements, and fail-closed handling that distinguishes prevented egress from confirmed exposure. |
| Trial-lead arm-map access logging and independent review timing | `OPEN-CONFIRMED-BUG` | Opus security finding `role-concentration-trial-lead` accepts the internal-trial role concentration only if the lead's arm-map access is logged and role 5 reviews it after dispatch-time access. Round 8 states only a one-time countersign. | Add an exact access-log population and post-access role-5 check without expanding the review round into broader roster continuity. |
| Natural-target minimum under cell underfill | `OPEN-CONFIRMED-BUG` | Opus failed check 4 and unresolved question 3 show that “at least 18 of planned 36” has no deterministic interpretation when an applicable cell underfills. Current code instead requires at least half of actual selected targets, but that behavior was not independently reviewed. | Select a proportional rule over actual selected cards with a per-applicable-case minimum and deterministic zero/underfill branches. |
| Exact malformed-output and provider finish-reason classification | `OPEN-CONFIRMED-BUG` | Opus failed check 4 finds `malformed output` undefined and the provider's “documented equivalent” finish reason absent from the frozen manifest; Codex also asks for unambiguous missing/zero semantics. | Freeze the accepted output grammar and per-provider finish-reason mapping, then test every mutually exclusive disposition. |
| Resource growth includes failed and retried calls | `OPEN-CONFIRMED-BUG` | Codex dissent 2 notes that accepted-call cost and time exclude failed/retried calls, allowing a treatment with more rejection or timeout burden to appear resource-eligible. Opus likewise asks how retries and failed attempts enter measured cost. | Measure all attributable attempts and their billed cost/observed duration per arm, with an explicit allocation rule for shared retries. |
| Calibration/person-hour package contains every permitted retry and preflight path | `OPEN-CONFIRMED-BUG` | Opus failed check 3 shows the stated calibration work already consumes most of the 12-hour package and one expressly allowed retraining/requalification path exceeds it before all preflights are included. | Recalculate the package from exact sheet counts and permitted branches; state whether package maxima are hard stops or only subordinate estimates under the hard total. |
| Human-role continuity and equally qualified alternates | `OPEN-CONFIRMED-BUG` | Opus failed check 8 finds eight single-holder roles with no substitution rule; one missed A/B deadline can shrink efficacy populations after spend. | Define prequalified named alternates and immutable replacement rules, or accept a deterministic stop rather than silent sample shrinkage. |
| Basis and falsification logic for efficacy, harm, and resource thresholds | `OPEN-CONFIRMED-BUG` | Opus failed check 6 and unresolved question 6 find no basis for `0.10`, `-0.50`, `-0.10`, token `35/55%`, or cost/time `30/50%` thresholds. Locked Q4 authorized round-two design resolution but supplied no values. | Give every threshold an owner-value, empirical, or arithmetic basis plus evidence that would change it; do not present unexplained constants as methodological facts. |
| Max-token forced-zero sensitivity for the primary efficacy endpoint | `OPEN-CONFIRMED-BUG` | Opus failed check 7 finds the brief requires a lane exclusion diagnostic but no equivalent efficacy recomputation, even though forced zeros can consume most of the `0.10` effect margin. Codex's cardinality finding also invalidates the stated 16-field forced-zero assumption across case types. | After scored output roles are fixed, report a predeclared efficacy sensitivity excluding max-token outputs alongside the inclusive decision rule. |
| Non-efficacy population and resource metrics are implemented and reproducible | `OPEN-UNTESTED` | Current `analyzeTrial` computes only family efficacy differences, selector eligibility, and the fixed root/package sequence. It does not implement round-8 lane, unsupported-claim, S3, incident, truncation, retry-inclusive resource, calibration, capacity, or continuity gates. | Treat the current CLI as partial implementation evidence only. Review each missing mechanism separately after foundational scoring and state semantics converge; this design round authorizes no code change. |
| Closed catalog, default-off objective opt-in, and dual-selector freeze in current code | `OPEN-UNTESTED` | Current config validation restricts three KStack-owned lens IDs, defaults the trial off, and requires explicit objective entries. Current core requires two distinct completed selector records and freezes their hash before packets are released. These align with locked Q5, Q8, Q9, and the round-8 direction, but no implementation review has accepted them. | Preserve as observed reality and do not relitigate locked answers. Validate behavior only in a later implementation/QC process after a design is approved. |
| Current code's scale constants and actual dispatch accounting versus round-8 trial plan | `OPEN-CONFIRMED-BUG` | Current code retains 16 cases, 8 people, USD 588, and 294 as hard upper bounds, but `prepareTrial` creates only 5 author dispatches per selected case and reserves USD 2 per dispatch. It has no modeled author-plus-Codex-plus-Opus 294-attempt plan, preflight/reserve/retry accounting, 138-hour cap, or day cap. Round 8's resource design and implementation therefore do not match. | Do not infer implementation authorization from matching headline constants. Revisit dispatch/resource accounting only after the trial endpoint and scored output roles are settled. |
| Pairwise uniqueness of `comparisonId`/`expectedRecordId` across the withheld manifest | `OPEN-CONFIRMED-BUG` | New in Round 12. Both reviewers independently found that no Round-12 validation rule requires `comparisonId` or `expectedRecordId` to be pairwise unique across the manifest's 162 entries, even though the outer inventory's per-record lookups (digest binding, roster binding, cardinality counting) presuppose a unique key. Opus's `BPL-R12-SEC-04` shows a manifest with two comparisons sharing one `expectedRecordId` would pass every other stated validation and let a submission bind to the wrong comparison undetected until the distinct-ID count blocks the batch. | Add an explicit pairwise-uniqueness check for `comparisonId` and `expectedRecordId` as a withheld-manifest validation item, rejected before freeze on any collision. |
| `recordId` assignment independence from tuple enumeration order | `OPEN-CONFIRMED-BUG` | New in Round 12 (Opus `BPL-R12-SEC-01`, genuine dissent). `recordId`/`expectedRecordId` are constrained only by a closed ASCII grammar with no rule against deriving them from, or making them monotonic in, the `(caseId, family, outputRole, candidateArm)` enumeration. This reopens a structural grouping/blinding channel distinct from the artifact-separation fix (see the structural-blinding-boundary row). | Require `recordId` assignment to be independent of the tuple enumeration order — for example, a fixed random permutation frozen with the manifest and never derivable from case/family/arm order. |
| Byte-differing duplicate judgment record disposition | `OPEN-CONFIRMED-BUG` | New in Round 12 (both reviewers, direct normative contradiction). Selected rule 3 items 1-2 keep the first-accepted record immutable and reject only a byte-differing second submission, letting an operator discard the erroneous one with "no version change." Normative fixture 5 and the failure-modes table instead say both records are rejected on a byte-differing duplicate. A conformant implementation cannot satisfy both statements at once, and neither statement says whether the expected record ID still counts toward the "exactly 162" completeness test while unresolved. | Pick exactly one disposition and make Selected rule 3, every fixture, and the failure table state it identically, including its effect on the completeness count. This is the single highest-priority isolated fix for the next round: both `BPL-R12-04` and `BPL-R12-05` cite it as a primary failure reason. |
| `raterId` authentication/binding to a submission | `OPEN-CONFIRMED-BUG` | New in Round 12 (Opus `BPL-R12-SEC-02`). `raterId` is self-asserted in the judgment record and checked only for roster membership; nothing binds a submission to the specific person who names that `raterId`, so any submitter able to produce a conformant record can claim to be any roster member. This undermines the attribution that the rater/resolver clustering dependence class and cross-record ordinal enforcement are meant to provide. | Define a submission-authorization or credential/transport-binding mechanism (implementation detail deferred) so that a future implementer knows `raterId` must be more than a self-asserted, membership-checked string; this is an implementation-parity item, not a schema change, and may be deferred like other implementation items already in this ledger. |
| `DUPLICATE_RECORD_AMBIGUOUS` as an unauthenticated denial-of-scoring vector | `OPEN-CONFIRMED-BUG` | New in Round 12 (Opus `BPL-R12-SEC-03`). Because any holder of a released sheet can construct a valid-content record with different Booleans for a known `expectedRecordId`, and Selected rule 3 item 2 renders such a pair non-scorable, an unauthorized submitter can force `DUPLICATE_RECORD_AMBIGUOUS` and potentially a new trial version with re-adjudication, with no stated submitter authorization or rate/scope control. | Define who is authorized to submit a judgment record for a given `expectedRecordId` (tied to the same authentication mechanism as the `raterId`-binding item above) before treating the duplicate-disposition rule as safe against adversarial submission. |
| `policyRefs` collation reference and uppercase-value scope | `OPEN-CONFIRMED-BUG` | New in Round 12 (Opus). Selected rule 1 item 5 requires `policyRefs` to be "sorted by the closed-ASCII collation in Selected rule 6," but Selected rule 6 (6a/6b/6c) never defines a collation function, and rule 1 item 1's identifier grammar is lowercase-only while `policyRefs` values `B`/`D`/`E` are uppercase and thus outside that grammar's stated scope even though the design also asserts "only identifier fields are ordered." | Define the exact byte-wise ordering function in Selected rule 6a and state explicitly which fields it governs, including `policyRefs`'s uppercase closed-set values. This is a self-contained wording/definition fix with no schema-shape change. |
| Judgment-record byte-size bound corpus justification | `OPEN-UNTESTED` | New in Round 12 (Opus). The 262,144-byte parser bound is asserted without a stated corpus-derived maximum for a judgment record (which embeds two full slot outputs plus 16 card text fields plus facts); if a legitimate sheet exceeds it, the record is rejected as malformed and, under the current cardinality rule, blocks the entire trial version. | Before a future round claims this bound final, measure or bound the actual maximum judgment-record size from representative case content and confirm it fits with margin, or adjust the constant. |
| Retroactive duplicate-revocation vs. per-rater ordinal no-gap invariant (dispute path) | `VALIDATED` | New in Round 13 (Opus `BPL-R13-SEC-01`, high). Round 13's retroactive revocation of a provisionally accepted record could leave a permanent, unfillable ordinal gap once a byte-differing duplicate arrived, making its own permanent-absence escape hatch unreachable in the exact case it existed for. Round 14 applied option (ii): never revoke the record or its consumed ordinals; mark the comparison disputed instead. Both reviewers independently confirmed via fixtures 7a/7b that a dispute under option (ii) cannot create an ordinal gap through the dispute path — this specific defect is closed and neither reviewer disputed it on its own terms (Opus's own recommendation was `BPL-R14-01: PASS`; Opus's material dissent explicitly scopes its disagreement to the claim being broader than the dispute path, not to the dispute-path claim itself). | None for the dispute path specifically. See the two new rows below for the residual ordinal-gap and first-submission-poisoning classes Round 14 exposed outside this path. |
| Ordinal-gap unfillability via the rejection/absence path (sheet presented, never ordinal-consuming) | `OPEN-CONFIRMED-BUG` | New in Round 14 (Opus `BPL-R14-SEC-01`, medium). If a rater adjudicated a sheet in presentation order but no submission for that `expectedRecordId` ever becomes ordinal-consuming (permanently rejected as `DIGEST_MISMATCH`/`MALFORMED_SHEET`, or the sheet is `ABSENT_ROLE_SHEET`), that rater's surviving ordinal-consuming records can legitimately carry a gap (e.g. `{1,2,4,5}`) and fail the no-gap test. An owner-authored absence declaration changes only scoring disposition (categories (a)/(b)); it has no provision excusing the missing ordinal, so the invariant stays broken and the trial version is permanently ineligible — round 14's fixture 6a implicitly assumes the absent sheet was never presented to any rater, an assumption the fixture does not state. This is the same `BPL-R13-SEC-01` failure class surviving outside the path round 14 fixed. | Isolate in a future round: either extend the absence declaration to also excuse the specific ordinal(s) a rater consumed in presentation order for a declared-absent `expectedRecordId`, or redefine the ordinal invariant so a rater's sequence is evaluated only over `expectedRecordId`s that were actually released to that rater and reached some disposition (accepted, disputed, or declared-absent), not a raw 1..N count. Verify against a fixture combining a declared absence with a presented-but-never-accepted sheet before combining with anything else. |
| Forged-but-valid first-submission permanent ordinal poisoning | `OPEN-CONFIRMED-BUG` | New in Round 14 (Opus `BPL-R14-SEC-02`, medium). Option (ii)'s never-revoke property means once any record becomes ordinal-consuming, its ordinals are counted forever — including a disputed record's. Combined with the still-open `raterId`-authentication residual (round 12), a single forged-but-valid first submission naming a real `rosterId` with an arbitrary `adjudicationOrdinal` permanently poisons that rater's sequence for the whole `trialVersionId`; neither a dispute, an absence declaration, nor a corrected resubmission removes it, and the only remedy is a whole new trial version. Round 13's revocable design did not share this specific defect. | Isolate in a future round, coupled to (but not solved by) the `raterId`-authentication item already in this ledger: define a submission-authorization mechanism before treating a first submission's ordinal-consumption as unconditionally trustworthy, or define a narrow correction path for a poisoned ordinal that does not require abandoning the whole trial version. |
| Uncapped absence-declaration surface compounding single-shot dispute griefing | `OPEN-CONFIRMED-BUG` | New in Round 14 (Opus `BPL-R14-SEC-03`, low). Anyone holding a released sheet and any valid `rosterId` can submit one valid, byte-differing record and irreversibly dispute a comparison with no un-disputing path; the only recovery routes are a full new trial version or an owner absence declaration, and the absence-declaration surface remains unbounded (round-13 deferred item e, still deferred through round 14). The interaction — griefing pressure toward increased use of an uncapped owner declaration — is not addressed by either round. | Address together with the already-deferred absence-declaration-surface-bounding item (round-13 item e) in a future round; do not treat them as independent, since bounding the declaration surface without addressing griefing pressure (or vice versa) leaves the same risk reachable through the other path. |
| Absence-declaration identity and succession (`declarationId`) | `OPEN-CONFIRMED-BUG` | New in Round 13 (both reviewers, direct schema defect). The `bpl-trial-version-absence-declaration` schema has `trialVersionId`, `supersedesDeclarationId`, and `declaredAbsences`, but no `declarationId` — so `supersedesDeclarationId` has no referent, "the declaration no other declaration's `supersedesDeclarationId` points at is current" cannot be evaluated, and nothing stops two or more declarations for one `trialVersionId` each carrying `supersedesDeclarationId: null` from being simultaneously "current" with no tie-break. Every downstream consequence (eligibility, reachability, propagation) depends on this unevaluable determination. **Round 15 (Opus, unresolved question): this defect now has a hard, previously-unrecorded dependency on `BPL-R15-01`'s new selection-record cardinality invariant** — `|category (a)| + |category (b)| = 162` requires "the current (non-superseded) absence declaration" to be formally identifiable, which is exactly what the missing `declarationId` makes impossible whenever more than one declaration could exist for a `trialVersionId`. Opus is explicit this is a pre-existing defect, not introduced by `BPL-R15-01`, but the coupling must be recorded so a future round does not treat the two as independently fixable. | Add a `declarationId` bound to the same closed ASCII grammar as other identifiers, required unique across declarations; require `supersedesDeclarationId` to be `null` or an existing `declarationId` for the same `trialVersionId`; require exactly one non-superseded (root/leaf) declaration per `trialVersionId` as a fail-closed check. Any future round fixing this must also re-verify `BPL-R15-01`'s cardinality invariant against the corrected schema, since the invariant's own definition of "current declaration" depends on this fix. |
| Owner-authored absence-declaration selection-bias/blinding surface | `OPEN-CONFIRMED-BUG` | New in Round 13 (Opus `BPL-R13-SEC-02`, medium). The permanent-absence declaration is authored by the owner, who also holds the withheld control manifest and therefore arm identity. Nothing caps the number/proportion of declared absences, requires per-arm/per-family balance diagnostics, requires the declaration to be authored only for an `expectedRecordId` with an actual recorded rejection, or requires independent review. A declaration can therefore convert an unfavorable comparison into a non-numeric excluded-from-`n` row without the audit record showing a violation. Codex separately notes (`BPL-R13-SEC-03`, low) that a declared `reason` is never cross-checked against the inventory's actually-recorded rejection reason for that `expectedRecordId`. | Bound the declaration surface: require a declaration to name only an `expectedRecordId` that already has a matching recorded rejection reason from the inventory (or an explicit, separately-documented unobtainable-source justification), and consider a balance diagnostic or second-party review before treating the mechanism as safe against selective declaration. |
| Unrecoverable whole-trial-version denial of service from a single poisoned ordinal (`BPL-R15-SEC-01`) | `OPEN-CONFIRMED-BUG` | New in Round 15 (Opus `BPL-R15-SEC-01`, medium). Selected rule 3's never-revoke guarantee (validated, `BPL-R14-01`) means an ordinal-consuming record's ordinals are counted forever. Revised Selected rule 4 item 3/4 (`BPL-R15-02`, both reviewers PASS on its own chartered terms) evaluates the per-rater ordinal gate over the *complete historical* ordinal-consuming set, including disputed and declared-absent records, and blocks the *entire* trial version on any one rater's violation. The combination means a single incorrect `adjudicationOrdinal` in any one of the 162 ordinal-consuming records — a clerical error, or forged via the still-open `raterId`-authentication residual (`BPL-R14-SEC-02`) — permanently and unrecoverably blocks that trial version's eligibility: a byte-differing correction only disputes the comparison (the bad ordinal stays counted), and an owner-authored absence declaration explicitly does not remove a counted ordinal either. The only stated remedy is minting an entirely new `trialVersionId`, i.e. re-running the full costly blinded adjudication. Opus's own framing: this is structurally the same failure class as round 13's `BPL-R13-SEC-01` — an escape hatch (dispute or absence declaration) asked to repair a state it cannot repair — recurring at one level up (an ordinal itself, rather than a record's content) now that `BPL-R15-02` makes the evaluation scope explicit. Opus does not dissent from `BPL-R15-02`'s resolution itself (calls it "forced by fixture 7b"); this is a consequence of the correctly-chosen resolution, not a flaw in the resolution's own wording. | Round 16's overlay and round 17's pre-display receipt cures are both rejected. The finding remains open; no further round is prepared. |
| Digest-bound, owner-approved ordinal correction and atomic selection freeze (`BPL-R16-01`) | `REJECTED` | Round-16 digest `2660d5ad66f57b313ab06fa1bfb91f6b15816b870c957e9b58452ca6f7f0ace1`, invocation `307ed223-26cf-4b08-9ffd-f93bea327cfb`: Codex 61 and Opus 72 both FAIL; combined 61. Seven findings and four dissents establish category-path races, unfenced/stale and crash-ambiguous selection, undefined digest/mapping/precedence, and an evidence blob that cannot prevent order falsification. | Do not patch or reuse. Rejected-options entry 5 records the narrower prevention-plus-point-in-time alternative. |
| Admission-time durable presentation-receipt binding (`BPL-R17-01`) | `REJECTED` | Round-17 digest `6e5ef835d7aaf006a508c934c1d0215b32280dacf5f4e1e6535bfc6309615829`, invocation `b0843f88-a3dc-4d5f-8207-fdd39c2c6fdf`: Codex and Opus both FAIL. Submitter-chosen ordinal prevention and corrected resubmission are sound sub-directions, but issued/unfilled receipts make permanent gaps likely; resolver prerequisites, null receipt cases, and receipt revision/retry binding are incomplete. | Do not use this mechanism. Any future alternative must give each issued presentation a mechanical terminal disposition without renumbering or hiding gaps. |
| Monotonic trial-state CAS and point-in-time analysis selection (`BPL-R17-02`) | `REJECTED` | Same reviewed digest/invocation: both reviewers FAIL. Exact revision/digest CAS removes round 16's lease ambiguity, but byte-exact state/snapshot preimages, resolver events, datastore evidence, and authoritative selection/supersession policy are missing; unrestricted reselection permits timing/outcome shopping. | Preserve exact CAS only as an unvalidated direction. A future design must define canonical bytes, cite the datastore primitive, and require audited authoritative supersession. |
| Exact canonical `trialSnapshotDigest` projection/preimage (`BPL-R18-01`) | `OPEN-UNTESTED` | Frozen round-18 digest `d8bab17711eb01b45f8a8115ad71ea89baa06faf4a51b682f6d3ba7dc6f93aff`. The candidate defines one root schema, exact receipt/ordinal/disposition/absence sections, sort keys, prior-selection treatment, compact UTF-8 bytes plus terminal LF, and SHA-256 formula. It explicitly leaves every other round-17 defect open. | Run one fresh independent review only if authorized. Require byte-identical cross-runtime fixtures and item-specific attribution; do not infer whole-mechanism acceptance from a PASS. |
| Selected rule 4's closing paragraph contradicts the `BPL-R15-02` resolution it was not updated for | `OPEN-CONFIRMED-BUG` | New in Round 15 (Opus failedCheck). Selected rule 4's closing paragraph — carried forward unrevised since round 12 — states "[a]ny file that fails step 1, 2, or 3 ... is never passed to the semantic scoring path" and that the affected `expectedRecordId`'s fate "depends entirely on whether the owner authors a permanent-absence declaration." Both framings are now false under `BPL-R15-02`'s explicit resolution: an ordinal violation is not a per-file, admission-time step-3 rejection (it is a trial-version-wide gate evaluated at eligibility-check time against the complete historical set), and a declared-absent record's ordinals remain counted regardless of the declaration, so the declaration cannot "decide" an ordinal violation's fate the way it decides a `DIGEST_MISMATCH` or `ROSTER_VIOLATION`'s fate. | Neither rejected round 16 nor 17 validates replacement text. Keep open. |
| No submission-close/freeze point before a selection record is written (post-selection dispute can retroactively invalidate it) | `OPEN-UNTESTED` | New in Round 15 (Opus unresolved question). `BPL-R15-01`'s `|category (a)|` is defined over the "currently scorable" set, which is time-varying; nothing in Selected rule 4 closes submissions at selection time. A byte-differing duplicate arriving after a selection record is written would shrink the true scorable set below the recorded digest-pair count, making an already-scored analysis version retroactively fail its own eligibility test under item 4's disjoint-union rule. | Round 16's close and round 17's point-in-time selection are rejected; official selection/supersession remains open. |
| Ordinal-invariant "a decrease" phrase is order-dependent against an otherwise set-valued, order-independent definition | `OPEN-CONFIRMED-BUG` | New in Round 15 (Opus failedCheck). `BPL-R15-02`'s explicit resolution defines the per-rater ordinal invariant as a property of the "complete historical ordinal-consuming set" — a set has no arrival order — yet the same sentence still rejects "a duplicate ordinal, a gap, or a decrease." "A decrease" presupposes an arrival-order sequence, leaving an implementer a live and consequential choice between (i) pure set semantics (no duplicate, no gap 1..n, order irrelevant) and (ii) an additional arrival-order monotonicity check that could reject an admission-order sequence whose resulting *set* is nonetheless gap-free. This is the same defect class (an order/per-record predicate smuggled into a stated global, order-independent property) round 14 was rejected for, now recurring one level down inside `BPL-R15-02`'s own corrective text. | Round 17 restated pure set semantics but failed as a mechanism; no isolated accepted fix exists. Keep open. |

## Source basis

The initial population was verified directly against the objective, locked
clarification record, Round-8 decision brief, both structured reviewer
envelopes, deterministic checks, gate, and current implementation in:

- `.kstack/reviews/broader-planning-lenses-2026-08-24-round8/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round9/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round10/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round11/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round12/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round13/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round14/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round15/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round16/`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round17/decision-brief.md`
- `.kstack/reviews/broader-planning-lenses-2026-08-24-round18/decision-brief.md`
- `.kstack/decisions/broader-planning-lenses-2026-08-24-rejected-options.md`
- `plugins/kstack/scripts/kstack-planning-lens-core.mjs`
- `plugins/kstack/scripts/kstack-planning-lens-trial.mjs`
- `plugins/kstack/scripts/kstack-config.mjs`

Round 8 has no `synthesis.md`; no resolution was inferred from that missing
artifact. Earlier-round lineage was checked against the Round-5, Round-6, and
Round-7 syntheses so recurring findings were not misclassified as newly
introduced. Round 12's evidence was verified directly against its own
`decision-brief.md`, `codex.json`, `opus.json`, `synthesis.md`, `checks.json`,
and `gate.json` (combined confidence 48, status `BLOCKED`), and against
rejected-options entry 2, rather than inferred from the round's aggregate
verdict alone. Round 13's evidence was verified directly against its own
`decision-brief.md`, `codex.json`, `opus.json`, `synthesis.md`, `checks.json`,
and `gate.json` (combined confidence 34, status `BLOCKED`), and against
rejected-options entry 3. Round 14's evidence was verified directly against
its own `decision-brief.md`, `codex.json`, `opus.json`, `synthesis.md`,
`checks.json`, and `gate.json` (combined confidence 76, status `BLOCKED`),
and against rejected-options entry 4. Round 15's evidence was verified
directly against its own `decision-brief.md`, `codex.json`, `opus.json`,
`synthesis.md`, `checks.json`, and `gate.json` (combined confidence 82,
status `BLOCKED`). Round 15 is not recorded in the rejected-options ledger:
it is the thread's new high-water mark, clearing the 78 floor, the 80 gate
tier, and the 81 working target, with both chartered items (`BPL-R15-01`,
`BPL-R15-02`) independently PASSed by both reviewers and zero material
dissent; its `BLOCKED` gate status reflects a newly surfaced,
item-adjacent finding (`BPL-R15-SEC-01`) and stale carried-forward text,
not a regression or a rejected mechanism.
Round 16 was verified directly from `decision-brief.md`, `manifest.json`, both
envelopes/raw reports, `checks.json`, `synthesis.md`, and `gate.json`: digest
`2660d5ad66f57b313ab06fa1bfb91f6b15816b870c957e9b58452ca6f7f0ace1`,
invocation `307ed223-26cf-4b08-9ffd-f93bea327cfb`, Codex 61, Opus 72,
combined 61, gate `BLOCKED`. Rejected-options entry 5 records it. Round 17 was
verified from the same full artifact set: digest
`6e5ef835d7aaf006a508c934c1d0215b32280dacf5f4e1e6535bfc6309615829`,
invocation `b0843f88-a3dc-4d5f-8207-fdd39c2c6fdf`, Codex 66, Opus 68,
combined 66, gate `BLOCKED`. Rejected-options entry 6 records it.
Round 18 has only its frozen, secret-clean brief at digest
`d8bab17711eb01b45f8a8115ad71ea89baa06faf4a51b682f6d3ba7dc6f93aff`;
no confidence or gate status is inferred.
