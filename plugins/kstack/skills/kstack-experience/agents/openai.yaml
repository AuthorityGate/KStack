interface:
  display_name: "KStack Product Experience"
  short_description: "Govern product UX, brand, and visual quality"
  default_prompt: "Use $kstack-experience to establish and validate this product's shared UX and brand contract."
policy:
  allow_implicit_invocation: true
