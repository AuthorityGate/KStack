All write and file-read tools are disabled in this session (`Write`, `Read`, `Glob`, `Bash`), so I can't persist this to `.kstack/qc/jira-reflector-round2/` myself, and I could not read round 1's lesson to confirm non-overlap. The rule is below for you to place.

## Round 2 lesson

> **NEVER let a bulk or blanket "dismiss all" resolution path operate across evidence of mixed provenance. Partition candidates by origin and permit blanket dismissal only for externally-derived, inferential matches, requiring evidence the system recorded about its own side-effecting actions to be resolved individually by naming the specific artifact it produced.**
>
> **Why:** because a blanket clear that reaches self-recorded evidence silently disarms every downstream gate that depends on that evidence.

**Constraint check:** starts with NEVER; two sentences; one-clause why; no mention of the specific command, Jira, or the submit gate; not about packet completeness; states the class plus a decision procedure (partition by provenance) rather than restating "categories it wasn't meant to touch."

**Two things I could not do, for you to close out:**
1. Verify this doesn't restate round 1's rule — read `.kstack/qc/jira-reflector-round1/` before filing. If round 1 already covered provenance-vs-blanket-dismissal, this needs revision rather than duplication.
2. Match the existing filename convention and the `.kstack/reflexion-lessons.json` entry schema when persisting.
