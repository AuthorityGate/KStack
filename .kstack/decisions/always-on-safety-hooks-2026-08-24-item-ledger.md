# Per-item ledger: always-on safety hooks redo lineage

**Thread:** `always-on-safety-hooks-2026-08-24`  
**Lineage covered:** round 11-redo onward  
**Created:** 2026-08-25  
**Status:** living document; update in place

## Purpose

This ledger rates individual technical items and claims independently of the
aggregate confidence or disposition of the round bundle in which they appeared.
It implements the owner's 2026-08-25 process correction: retain useful
item-level evidence even when a bundle is rejected, and do not treat a bundle's
verdict as the status of every sub-claim.

Commit `157276e` updated
`plugins/kstack/skills/kstack-design/SKILL.md` to require each post-finding round
to address the smallest coherent, independently reviewable change, ideally one
finding and its fix, with only a genuinely inseparable minimal-pair exception.
Accordingly, a remedy that reviewers liked only inside an overloaded round
remains `OPEN-UNTESTED` unless the evidence isolates it well enough to attribute
the result. This ledger complements, and does not replace, the whole-mechanism
rejected-options ledger in
`always-on-safety-hooks-2026-08-24-revert-to-round10-2026-08-25.md`.

## Status meanings

- `VALIDATED` — item-specific evidence accepts the claim or classification;
  nothing further is required for that exact item.
- `REJECTED` — item-specific evidence establishes that the proposed claim or
  mechanism is unsound; the replacement is named in **Next action**.
- `OPEN-UNTESTED` — a proposal or disposition exists, but it has not been
  isolated and tested under the one-change-per-round rule.
- `OPEN-CONFIRMED-BUG` — reviewers confirmed the underlying defect or design
  gap, but no isolated accepted fix exists.

## Maintenance rule

Every future round dispatch prompt for this thread must read this ledger first.
After synthesis, update in place the status and evidence for every item that
round addressed, add every newly discovered item, and name the next isolated
round action for anything still open. Preserve material evidence when status
changes; do not recreate the file, infer sub-item status from a bundle's gate
result, or let this ledger go stale.

## Item ledger

| Item | Status | Evidence | Next action |
|---|---|---|---|
| Absolute hook-module path; remove cwd, relative, environment-root, and runtime `PATH` fallbacks | `VALIDATED` | Round12-redo4 Codex and Opus both agreed that the literal absolute hook-module operand removes the original `.`/cwd fallback; the synthesis says this closes `KSG-R12R3-001` for that operand. Round12-redo5 Codex and Opus again called absolute `N`/`H` pinning complete for qualified cells. Neither disputed this narrow sub-claim. | Nothing further for target selection itself. Do not broaden this validation to quoting, interpreter provenance, host qualification, loader state, or pre-start failure behavior. |
| Launcher command construction with no quoting/escaping or path-rejection contract | `REJECTED` | Round12-redo4 Codex `KSG-R12R4-QUOTE-001` called “correctly shell-quoted” undefined. Opus's finding of the same ID showed that double quotes leave dollar, backtick, and backslash interpretation active under POSIX `sh`. Rejected-option 8 records the mechanism-level rejection. | Replace with an exact consumer-specific encoder or an exact path-rejection grammar, plus parser-significant path fixtures. |
| Full round12-redo5 launcher-safety mechanism: absolute pinning, exact encoders, interpreter checks 1-4, and deployed use | `REJECTED` | Round12-redo5 accepted the absolute tuple and encoders for their declared consumers, but Codex `KSG-R12-HOST-BINDING-001` and Opus `KSG-R12R6-A`/`D` found no binding between publication/status and the deployed host build, Claude command form, Codex shell, or requalification event. Combined confidence fell to 68 and rejected-option 9 records the mechanism rejection. | Prefer the recorded alternative: a KStack-owned executable shim with fixed zero-argument denial, plus an exact host/platform/handler/shell qualification record and update invalidation. |
| `argv0` must be absolute and byte-equal to canonical interpreter `N` | `REJECTED` | Round12-redo5 Opus `KSG-R12R6-C` and dissent 2 found caller-controlled `argv0` forgeable by the modeled same-UID actor, redundant with checks 1-4 for non-forged cases, and hostile to normal shebang, npm, symlink, and likely macOS layouts. The synthesis directs dropping the rule. | Drop it as a security control; state provenance over canonical `process.execPath`, executable identity, ownership, ancestor ownership, mode, and minimum Node version instead. |
| Wording that the launcher provides “complete interpreter identity” | `REJECTED` | Round12-redo5 Opus dissent 1 said only the interpreter binary is pinned while login-shell loader variables can select pre-hook code. Codex separately recorded `KSG-DEFER-LOADER-001` and recommended keeping the loader limitation explicit. The synthesis says both reviewers narrow the claim. | Say “interpreter-binary identity” only and disclose loader/preload state until that separate item is resolved. |
| Host command-form drift can leave a guardless cell reported active | `OPEN-CONFIRMED-BUG` | Round12-redo5 Codex `KSG-R12-HOST-BINDING-001` and Opus `KSG-R12R6-A`/`D` confirm the gap. Opus dissent 3 explains that this is successful launch without the guard, not ordinary pre-start failure, and therefore cannot be deferred to the existing Q4/Q14 residual. | A single-change round must test deployed consumer qualification and invalidation, including ignored Claude `args`, incompatible Codex parsers/shells, and host auto-update drift; compare that with the fixed-deny shim alternative. |
| Totalizing closed-artifact-universe capability analysis | `REJECTED` | Round12-redo Codex `KSG-DESIGN-012-01` found the semantic oracle non-reproducible. Opus found that exception-free analysis of all bundled/transitive code, including `pglite`, `node-gyp`, native, and Wasm paths, could make every channel unshippable. Rejected-option 5 records the confidence regression from 74 to 66. | Scope future analysis to the actual safety boundary and finite KStack-authored rules; do not revive universal third-party/dependency analysis. |
| Narrow safety-module capability analysis with ambiguous roots and launch closure | `REJECTED` | Round12-redo2 Codex `KSG-DESIGN-LAUNCH-001` found the non-ESM launch boundary unspecified. Opus found no normative root set and observed that `hooks.json` alone reaches only hook and matchers. Rejected-option 6 records the incomplete launch, spawn, upgrade, and wire boundary. | Use explicit roots and a finite, decidable boundary, then isolate that boundary from unrelated fixes in a future round. |
| Install-time arbitrary KStack-file loophole and choice of cryptographic pinning versus a plain file-list check | `OPEN-CONFIRMED-BUG` | Round12-redo9 reviewed this row together with the inseparable same-name digest row at design digest `af9a4af351858d9008dceed67ae7fb22f28fc60375adfc99b46f4c1a8df1aa33`. The packet inlined an exact 1,827-entry full-payload list, paired every path with raw-byte SHA-256, detached the expectation to avoid self-hashing, and defined the selected-root digest as SHA-256 of 308,277 canonical manifest bytes. Both reviewers retained the plain mechanism and requested no signing or external pin, but Codex revised at 58 and Opus at 66; combined confidence 58 fell 20 points below the 78 high-water mark. Codex `KSG-R12-01` through `03` and Opus `KSG-REV-01` through `04` found incomplete root/parent traversal, canonical path/JSON rules, filesystem aliases, resource/diagnostic bounds, first-install and checkpoint-only claims, payload freezing, legacy migration, and crash-atomic state. The round-12 gate was `BLOCKED` with seven security findings and six genuine dissent entries. Rejected-option 12 records the rejected exact design. | Keep the plain exact-list-plus-SHA-256 direction, but a future owner-authorized round must start from a preassembled immutable payload; use one printable-ASCII canonical path/JSON grammar; name supported filesystem cells; traverse from stable root/parent handles without following links; bind work and diagnostic limits with typed capacity failure; and make selector, manifest, digest, and registration one recoverable versioned transition. Keep first-install provenance and between-check same-UID replacement explicitly outside Q4 detection; do not add signing or external pinning absent a separately named need. |
| Repository-local Git config isolation: empty global/system config and neutralize includes/helper-spawning keys | `VALIDATED` | Round12-redo6 isolated this item with design digest `5a8c80f6f23e619e960e4667e8e9445b671b062de1332537ac3ef8176d192fb9`. The selected worker-derived mechanism makes a fresh KStack-owned bare scratch repository the child's `--git-dir`, points `GIT_OBJECT_DIRECTORY` at the verified target object store, scrubs ambient `GIT_*`, sets `GIT_CONFIG_SYSTEM=/dev/null`, `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_CONFIG_NOSYSTEM=1`, and `GIT_CONFIG_COUNT=0`, and permits only exact scratch-local plus named command-scope values. Codex approved at 92 with no findings or dissent. Opus scored 78 with no material dissent and expressly agreed that this structurally removes target local/worktree config and arbitrary `include`/`includeIf` from Git's configuration graph; its revise findings concern operand/format equivalence, foreign ownership, version scoping, TOCTOU, temporary-parent selection, and test-harness containment rather than restoration of target-config authority. Combined confidence 78 exceeded the owner's 74 item-validation high-water rule. The full round-12 gate remained `BLOCKED` at the configured 80 floor, so this item validation is not full-design or implementation approval. | Nothing further for the exact target-local/worktree config-source closure. Preserve the round12-redo6 implementation cautions and do not broaden this validation to Git binary/version support, repository ownership/object-store TOCTOU, test seams, executor/worker sites, launcher identity, or the admin credential-helper surface. |
| Git binary canonical-path pinning, supported layouts, minimum version, and no-lazy-fetch conformance | `OPEN-CONFIRMED-BUG` | Round12-redo7 isolated this item at design digest `02a8b709ace3b33efebdce24713c435820b75467fc9b0e5e772bd036ebc283fa` and confirmed that Git 2.45.0 is the correct feature floor and that fixed candidates plus dual-chain realpath/root-trust validation improve literal `/usr/bin/git`. The complete option was rejected at combined confidence 42 (`min(Codex 42, Opus 64)`, down 36 from the 78 high-water mark). Codex `KSTACK-R12R8-001`/`002`/`003` and Opus `KSG-REV-ENV-001`/`002`, `KSG-REV-EXE-003`, and `KSG-REV-NET-004` show that the denylist inherits proxy/toolchain/runtime selectors, the loopback canary can egress elsewhere or false-pass, Darwin `/usr/bin/git` can dispatch through `DEVELOPER_DIR`, wrapper leaves do not bind the effective program, and pathname recheck does not bind the inode executed by `spawnSync`. Both reviewers also found unresolved mixed-candidate precedence, broker-lifetime/cost, platform/version coverage, version grammar, and installation-wide unavailable surfacing. The round-12 gate was `BLOCKED` with 9 security findings and 6 genuine dissent entries. Rejected-option 10 records the mechanism rejection. | Keep Git 2.45.0 and the redo6 config closure, but do not reuse the rejected runtime loopback/denylist design. A future single-item alternative must use a closed child-environment allowlist, either descriptor-bind a native executable on an honestly narrowed platform or specify another atomic exec identity primitive, observe/deny all egress rather than one listener, totalize candidate-failure precedence, prove broker/qualification lifecycle and supported-layout coverage, and surface scanner unavailability through the separately required status-v2 dimension. |
| Exact-builder-bound preview cap checked before scan while containing post-scan values unchanged | `REJECTED` | Round12-redo Codex `KSG-DESIGN-012-02` and Opus failed check 3 found the timing contract impossible because the final READY/ask buffers contain scan-derived values unavailable before scan. Rejected-option 5 records the self-contradiction. | Replace with independently derived conservative input/output bounds and post-scan measurement of the actual final serialization; do not bind acceptance to builder identity. |
| Plain conservative numeric preview cap without builder identity | `OPEN-CONFIRMED-BUG` | Round12-redo11 reviewed this item at digest `9b7de18f2c419c86005ac3af06580cfefe0de504017b4391ccec4e9f2538c675`. Codex and Opus displayed per-item PASS, but Opus's own failed check established that the admitted 64-byte ASCII `matcherVersion` can contain quote, backslash, or controls and expand under JSON serialization. Therefore the proposed 1,253-byte attestation, 1,456-byte empty-preview READY, and 3,946-byte READY cap are not conservative for the stated grammar. Primary-artifact synthesis resolves that internal contradiction against validation. The gate was BLOCKED and combined confidence 55 was 28 below high-water 83. | Keep final post-scan Buffer measurement and the 2,048/4,096 hard ceilings, but either restrict `matcherVersion` to a no-escape ASCII alphabet and recompute, or include maximal JSON escape expansion in a new numeric derivation. Do not restore builder identity. |
| Broker-unavailable versus atomic-cancellation precedence | `VALIDATED` | Round12-redo11 Codex and Opus both gave this item PASS. For an otherwise valid live multi-ref handle, public `execute` consumes once and returns fixed `KSG-BROKER-UNAVAILABLE-001` before reading atomic/current/downstream state; the pure future helper separately returns `CANCELLED(ATOMIC_UNAVAILABLE)` unless support is literal true. The shared logical fixtures distinguish both domains with zero downstream spies. Opus retained a low consequence about handle-state distinguishability/burn, but did not dispute the requested precedence. | Nothing further for unavailable-versus-atomic ordering. Preserve the consume-first oracle consequence as an implementation/UX caveat; changing that order is a new decision. |
| `createHostBridge` disposition: remove outright versus frozen unavailable shim | `VALIDATED` | Round12-redo10 isolated this item at design digest `10bf44b92aca5a30d73a0163b832805ff10ca026379eff266157fc139beb38a1` and definitively selected outright removal. No named/effective export, instance/prototype/static property, symbol, alias, getter, proxy, or closure factory remains; stale imports/calls fail with ordinary ESM/JavaScript `SyntaxError`/`TypeError`/`ReferenceError`, never a KStack result. Both reviewers approved: Codex 92 and Opus 83, so combined confidence 83 exceeded the 78 high-water mark by 5; both material-dissent arrays were empty. The frozen shim was rejected because it preserves a misleading channel-shaped attachment point, duplicates public unavailability, invites retry/fallback, and gives callers no functional compatibility. The round-12 gate was `BLOCKED` despite both approvals because four retained findings, six Opus implementation-acceptance questions, and two deterministic checks remain; these concern current pre-implementation exposure, fresh-realm/prototype assignment, token/AST scan scoping, canary observability, static-link fixtures, release evidence, and capable-test sequencing, not the disposition. | Nothing further for removal versus shim. In implementation acceptance, require the marked CFD-v1 breaking release/migration note, a clean-child static-import `SyntaxError` fixture, explicit production/negative-evidence path sets and named AST tooling, fresh-realm or separately authorized prototype freezing for pollution tests, globally observable zero-effect evidence without adding production seams, generated-package checks, and explicit recording of the interim capable-path test-coverage consequence. Do not restore this identifier; any future sanctioned owner host requires a new reviewed API name. |
| Git-history reconstruction adds no capability to the modeled same-UID actor | `VALIDATED` | Round11-redo Codex and Opus accepted the owner-settled classification: historical bytes remain reconstructable, but reconstruction grants no capability the arbitrary-code same-UID actor lacks; credential validity is a separate residual. Every later synthesis preserved this without reopening it. | Nothing further; retain the Q3 capability versus credential-validity distinction. |
| Same-UID post-install replacement/TOCTOU belongs to Q4 detect-only and Q14 outside coverage when KStack adds no capability | `VALIDATED` | Round11-redo Opus `CFD1-POSTINSTALL-REPLACEMENT` explicitly called this disclosed and “not a defect.” Round12-redo4 Opus `KSG-R12R4-TOCTOU-001` again treated it as a disclosed residual, and neither reviewer dissented from the classification. | Nothing further for the classification; keep the exact scope and consequence visible in status/help/docs. |
| Exact graph root set `{hook, broker, admin}` | `VALIDATED` | Round12-redo11 Codex and Opus both gave this item PASS. The exact roots are hook, broker, and admin; their union reaches four modules including matchers. Launch metadata reaches hook, whose complete launch-time closure is only hook plus matchers; broker/admin are independent direct-import/CLI roots. This explicitly resolves the earlier 2-of-4 gap without adding a launch edge. Opus's import-side-effect finding concerns safe implementation of the accepted admin-to-hook edge, not root enumeration. | Nothing further for the exact roots/union/launch-closure distinction. Implementation acceptance must make hook import inert or main-gated before admin imports it. |
| Base Git spawn hardening: `shell:false`, fixed subcommands/flags, `--`, operand validation, minimal environment, no ambient `GIT_*` | `OPEN-UNTESTED` | Round12-redo2 Opus `KSG-SCAN-SPAWN-001` confirmed the unconstrained spawn. Round12-redo3 supplied the base argv/environment contract and injection fixtures, but the bundle was rejected because local config and version behavior remained open. | Review the base spawn contract together only with the genuinely inseparable local-config/version controls, using argv/env capture and option/environment/network canaries. |
| Plain full installed-file list as the small replacement for five-name checking | `OPEN-UNTESTED` | Round12-redo3 defined a sorted exact path list, recursive set equality, no globs/symlinks/non-regular entries, and checks at setup/doctor/status/refresh/rollback. The synthesis says this ten-item bundle was rejected because the final list/classification and selected-root digest were deferred; the file-list mechanism was never isolated. | Materialize the exact list and test extra, missing, duplicate, escaping, symlink, and differently named first-party capable files in a single-change round. |
| Same-name content overwrite and selected-root content-digest definition | `OPEN-CONFIRMED-BUG` | Round12-redo9 reviewed this row as the other half of the exact-list mechanism at design digest `af9a4af351858d9008dceed67ae7fb22f28fc60375adfc99b46f4c1a8df1aa33`. It defined each file digest over exact raw bytes and the selected-root digest over canonical detached-manifest bytes, with a direct same-name matcher-module overwrite fixture. Both reviewers agreed per-file SHA-256 is the correct small content-binding direction, but the general canonicalization was not unique for every admitted path/number spelling, parent traversal and host alias behavior were incomplete, verification work was unbounded, and detached-manifest/registration selection lacked a complete bootstrap, migration, and crash-recovery transaction. Combined confidence 58 (`min(Codex 58, Opus 66)`) regressed 20 points below 78, so the exact option was rejected and the gate remained `BLOCKED`; rejected-option 12 and round12-redo9's accepted reflexion lesson preserve the evidence. | Retain raw-byte SHA-256 per listed file and a root digest over one canonical manifest, but first close the canonical byte grammar, immutable payload stage, stable root-handle traversal, supported-filesystem alias policy, bounded verification, independently supplied legacy expectation, and recoverable state tuple. Test same-name replacement and content restoration without introducing signatures or external trust infrastructure. |
| Semantic first-party protected-path analysis over KStack-authored files | `REJECTED` | Round12-redo3 Codex `KSTACK-R12-003` and Opus failed check 5 found no finite parser, source/sink, indirect-flow, generated/embedded-code, or deterministic acceptance rules. The synthesis says this repeated a smaller form of the rejected semantic-analysis problem. | Replace with a finite named protected-path lint or narrow/disclose the claim; do not use an open-ended semantic capability oracle. |
| Finite named protected-path lint over KStack-authored permitted files | `OPEN-CONFIRMED-BUG` | Round12-redo11 Codex and Opus both failed this item. Undefined AST categories/exception nodes made the rule set non-exhaustive; mutation matching either requires disclaimed data-flow inference or is vacuous; and locator/mutation literals can reject the deny classifier that must recognize them. Both retained the finite syntactic direction, but the exact reviewed option is rejected at combined confidence 55 versus high-water 83. | Anchor every rule to exhaustive Acorn node/callee/member patterns; choose an inline-array AST anchor or whole-file literal over-approximation; define digest-bound data-table/exception nodes; and require a conforming deny-classifier positive fixture. Keep third parties excluded and make no semantic capability claim. |
| Structurally remove constructor executor/scanner/state/callback injection seams | `OPEN-CONFIRMED-BUG` | Round12-redo11 Codex failed and Opus passed. Both accepted the zero-argument factory/private classes/exact facade/negative structural fixture direction, but Codex found no finite algorithm for alternate implementation, indirect construction, nonlocal import, or reachability. Combined confidence 55 fell below 83; the exact verification option is rejected, not the structural objective. | Replace semantic reachability with exact import/export/call-expression node sets and forbidden parameter/property/callee identifiers, plus one negative AST fixture per indirect form. Preserve failure/clock/timeout coverage with process-level fixtures, not production injection seams. |
| Status v2 must distinguish fast-hook activation from broker/execution availability and have reachable complete producers | `OPEN-CONFIRMED-BUG` | Round11-redo Opus found no discriminator or migration contract. Round12-redo added v2 but left explicit `schemaVersion:1` ambiguous. Round12-redo3 made enabled aliases semantically narrower, while Codex and Opus found disabled/untrusted/error/per-scope shapes and producer ownership incomplete or unreachable. | Isolate the full canonical status state table, version handling, producer module, consumer migration, scanner/launcher-unavailable dimensions, and literal messages. |
| Verify-before-select upgrade with multi-scope atomicity, crash recovery, and final reread | `OPEN-CONFIRMED-BUG` | Round12-redo initially closed the silent manual-refresh window but Codex `KSG-DESIGN-012-03` found partial multi-project refresh/rollback inconsistent. Round12-redo2 found selection could precede successful refresh. Round12-redo3 both reviewers found no storage unit, journal, cross-scope atomic mechanism, or crash/partial-failure state. | Review a per-scope verify-before-swap protocol with explicit partial failure, durable recovery, rollback, and final reread; do not claim one atomic write across separate scopes/filesystems. |
| Loader/preload variables can run code before the hook's first instruction | `OPEN-CONFIRMED-BUG` | Round12-redo5 Codex `KSG-DEFER-LOADER-001` and Opus `KSG-R12R6-B` confirm that `NODE_OPTIONS`, `NODE_PATH`, login-shell profiles, and loader flags can select pre-hook code while interpreter checks still pass. It was explicitly deferred, not disproved. | Give launch-environment/loader isolation its own round; test both Claude direct exec and Codex login-shell ordering, and keep status/claim wording narrow until then. |
| Hook missing/error/nonzero/timeout and pre-start failure must have exact fail-closed host behavior | `OPEN-CONFIRMED-BUG` | Round12-redo3 Opus failed check 8 found the two-second hook error/timeout semantics unspecified. Round12-redo4 Codex asked whether host failure blocks or continues. Round12-redo5 Codex `KSG-DEFER-PRESTART-001` and Opus timeout findings confirm this remains unresolved. | Isolate real-host missing target, start failure, nonzero exit, malformed output, and timeout cases for every claimed cell; bind results to status and operator recovery. |
| Four distribution channels each need evidence rather than inference from source/setup | `OPEN-CONFIRMED-BUG` | Round12-redo11 narrowed claims honestly: source identified, current setup-copy path evidenced, Claude installed payload not evidenced, and simulated Codex-cache path not final-CFD-evidenced. Opus passed. Codex failed future promotion because digest algorithm/canonical manifest, file universe, and mandatory rejection of extra/missing/changed artifacts were absent. Combined confidence 55 fell below 83, so the current labels are retained but the exact promotion contract is rejected. | Keep per-channel non-propagating labels. Define SHA-256 over one canonical path-plus-byte-digest manifest, exact physical root enumeration, mandatory mutation rejection, whether NOT_EVIDENCED channels may ship, and a separately owned user-visible surfacing path. |
| Automatic post-update activation refresh closes the silent trust window | `OPEN-UNTESTED` | Round11-redo Opus `CFD1-UPGRADE-TRUST-WINDOW` confirmed the manual-refresh gap. Round12-redo selected automatic idempotent refresh and both reviewers said the silent window was closed, but the same bundled packet introduced unresolved enrollment enumeration and transactional rollback gaps. | Test automatic refresh as a narrow item after the per-scope state/index and rollback protocol are exact; preserve explicit disablement and surface failures immediately. |
| Admin credential path-format helper performs no credential read, stat, or existence probe | `OPEN-CONFIRMED-BUG` | Round12-redo Opus `CFD12-SEC-01` found the helper remained a credential-locator surface and that the acceptance plan observed only the adversarial child, not the sanctioned helper itself. Round12-redo8 isolated a dedicated `node:path`-only formatter and directly invoked the sanctioned export under throwing filesystem spies, but the option was rejected at combined confidence 72 (`min(Codex 72, Opus 76)`), six points below the 78 high-water mark. Codex `CFD12-SEC-01-CANARY-GAP` showed that exact-path traps miss swallowed parent-directory enumeration, glob, or watch probes. Opus `R12R8-SEC-01` through `04` found root-relative Windows admission, undisclosed textual-alias limits, an unbounded input-error channel, and dependence on a structural sandbox whose required runner flag was unspecified; it also found incomplete caller, executor, and skipped-fixture checks. Both reviewers retained the dedicated pure module as the preferred shape, but no isolated accepted fix exists. | Retain the dedicated formatter but redesign the canary to normalize every Node PathLike and forbid observation of the unique credential fixture root through exact paths, ancestor enumeration, encompassing glob patterns, and watches, with swallowed-probe negative controls. Admit only exact fully-qualified Windows drive/UNC bases; run the actual-module sandbox as an explicit hard Node >=20 command with its required flag; fail malformed input before mutation with a fixed path-free error; report every fixture cell; structurally prove both primitive caller records; canary executor construction up to the authorized worker-read boundary; and disclose unsupported textual aliases. |

## Source basis

The initial population was verified directly against both reviewers' structured
JSON and each synthesis in:

- `.kstack/reviews/always-on-safety-hooks-2026-08-24-round11-redo/`
- `.kstack/reviews/always-on-safety-hooks-2026-08-24-round12-redo/`
- `.kstack/reviews/always-on-safety-hooks-2026-08-24-round12-redo2/`
- `.kstack/reviews/always-on-safety-hooks-2026-08-24-round12-redo3/`
- `.kstack/reviews/always-on-safety-hooks-2026-08-24-round12-redo4/`
- `.kstack/reviews/always-on-safety-hooks-2026-08-24-round12-redo5/`
- `.kstack/reviews/always-on-safety-hooks-2026-08-24-round12-redo11/`

The whole-mechanism dispositions and owner-directed alternatives were checked
against
`.kstack/decisions/always-on-safety-hooks-2026-08-24-revert-to-round10-2026-08-25.md`.

## Round12-redo11 authoritative row override

The active safety hook refused an in-place textual replacement of the main
table's fourth requested row because its command-shaped title was classified as
a covered action while enrollment was `UNTRUSTED`. The control was not bypassed.
This in-file override is therefore the authoritative current row for that item:

| Item | Status | Evidence | Next action |
|---|---|---|---|
| Fourth requested item: base child-invocation hardening layered on redo6 | `OPEN-CONFIRMED-BUG` | Round12-redo11 Codex failed because the exact child API, literal global controls, full environment values, and numeric timeout/output/argv limits were not all enumerated; its medium finding says divergent implementations could claim conformance. Opus passed the decomposition but independently required numeric limits and per-platform fixed environments. Combined confidence 55 fell 28 below high-water 83. Redo6 configuration-source isolation remains validated and unchanged. This override supersedes the stale `OPEN-UNTESTED` main-table cell. | Inline every literal prefix/environment element and numeric resource constant, capture them byte-for-byte, define one fixed environment table per supported platform, and keep executable/version/atomic identity separate. |
