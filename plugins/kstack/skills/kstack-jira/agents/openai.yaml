interface:
  display_name: "KStack Jira Drafting"
  short_description: "Draft an offline Jira queue item"
  default_prompt: "Use $kstack-jira to draft a local Jira queue item without contacting Jira."
policy:
  allow_implicit_invocation: false
